# 项目状态

## 文件作用

这是跨窗口、跨会话同步项目状态的唯一入口。新会话开始后，必须先阅读本文件，再检查 Git 状态、最近提交、当前阶段计划和相关测试，不能仅凭本文件假设代码现状。

## 项目目标

构建一个面向 Agent 与确定性程序任务的可靠运行时和调度平台。

平台接收一个由多个步骤组成的工作流，按照步骤之间的依赖关系调度任务，并负责记录状态、分配执行节点、处理失败、限制资源以及展示执行过程。

一句话解释：

> 让普通程序和 Agent 组成的多步骤后台工作，在可能超时、失败、重启或节点失联的情况下，仍然能够被可靠调度、恢复和审计。

## 已批准范围

项目主干包括：

- 工作流定义与依赖关系；
- 单任务和多任务执行；
- 单 Agent 和多 Agent 执行模式；
- Agent Runtime、模型适配、工具权限、预算和人工审批；
- 模块 3 必做的自然语言工作流草稿，经过校验和用户确认后才能执行；
- 持久化任务状态；
- 重试、超时、取消、恢复和故障转移；
- 执行节点管理和资源控制；
- 日志、指标、调用链和审计；
- Agent 受限执行场景；
- 使用不包含公司信息的公开场景验证 Agent 与普通程序任务。

产品主线不是“强制多个 Agent 并发”，自然语言生成也不能绕过可靠执行内核；平台根据任务特征选择单 Agent、多 Agent 或普通程序任务，并对整个执行过程进行可靠管理。模块 3 必须提供自然语言到结构化工作流的最小演示。

## 非目标范围

当前不以以下目标为项目承诺：

- 替代所有程序员或完全自动完成软件项目；
- 支撑亿级用户或大规模生产集群；
- 自研数据库、消息队列、容器运行时或 Kubernetes；项目会使用并学习 Kubernetes，但不重复实现 Kubernetes 本身；
- 自研基础大模型；
- 安全运行任意恶意代码；
- 完整实现商业广告竞价或复杂推荐算法；
- 保证项目能够商业化或达到特定薪资水平。

## 当前阶段

- 阶段：模块 3“Agent Runtime、模型与工具执行”的实现、自动化验证、人工审核、Git 提交和学习文章发布均已完成；全仓文档与文章可读性整改也已完成并提交。
- 状态：模块 3 已全部收尾；模块 4“多 Worker、租约、心跳与故障恢复”尚未开始实现，下一步先进行需求与边界分析。
- 当前目标：根据项目路线、架构基线和已有单实例恢复语义，分析模块 4 的需求、技术边界、接口和验收证据；用户确认后再编写详细实施计划。
- 当前已有业务代码：单机工作流内核、FileStore、本地 CLI、PostgreSQL Repository、应用服务、HTTP API、认证、恢复协调器、Go SDK、控制面 CLI、Agent Runtime、Mock Model、HTTP 模型适配器、只读任务目录、草稿校验与确认 CLI，以及对应测试。

## 博客专栏与文章状态

- 专栏名称：从0构建AI Workload Platform工程实践；
- 专栏状态：已创建并公开，地址为 <https://blog.csdn.net/ylj__/category_13198549.html>；
- 项目总览篇：`从 0 构建 AI Workload Platform（一）：为什么做、做什么、与现有产品有什么不同`，状态为“已发布”，正文位于 `docs/学习/文章/模块0-项目总览.md`，公开地址为 <https://blog.csdn.net/ylj__/article/details/163590800>；用户内容审核、结构检查、内部内容检查、官方资料核对和公开可读性检查均于 2026-08-08 通过；
- 技术学习篇：`从 0 构建 AI Workload Platform（二）：工作流、DAG、状态机与可靠执行基础`，状态为“已发布”，正文位于 `docs/学习/文章/模块0-工作流-DAG-状态机与可靠执行基础.md`，公开地址为 <https://blog.csdn.net/ylj__/article/details/163591249>；用户内容审核、技术深度检查、内部内容检查、官方资料核对和公开可读性检查均于 2026-08-08 通过；文章明确说明尚无业务代码，实验内容为后续验证计划；
- 技术学习篇（模块 1）：`从 0 构建 AI Workload Platform（三）：单机可靠工作流内核如何设计与验证`，状态为“修订版已同步发布”，正文位于 `docs/学习/文章/模块1-单机可靠工作流内核设计与验证基础.md`，公开地址为 <https://blog.csdn.net/ylj__/article/details/163779494>；修订稿保留原有概念、数据结构和选型讲解，并根据模块 1 最终提交补充实际调度与恢复顺序、代表性自动化测试、CLI、五轮性能基线和真实限制；用户于 2026-08-19 完成修订版发布；
- 用户已确认当前不新增模块 1 第四篇：修订后的第三篇已经形成从设计到验证的完整主线，另写实践篇会与第 9、12、13 节大量重复；后续只有出现独立主题和新的学习价值时才重新评估；
- 技术学习与实践篇（模块 2）：`从 0 构建 AI Workload Platform（四）：从单机工作流内核到可靠控制面`，正文位于 `docs/学习/文章/模块2-从单机工作流内核到可靠控制面.md`；控制面、事务持久化、幂等和恢复属于同一条技术主线；技术事实审核、独立编辑审核和用户最终阅读于 2026-08-22 完成，用户已发布，公开地址为 <https://blog.csdn.net/ylj__/article/details/163975765>；
- 技术学习与实践篇（模块 3）：`从 0 构建 AI Workload Platform（五）：Agent Runtime、工具权限与自然语言工作流`，正文位于 `docs/学习/文章/模块3-Agent运行时模型工具与自然语言工作流.md`；技术事实审核、独立编辑审核和用户统一阅读已完成，用户于 2026-08-24 发布，公开地址为 <https://blog.csdn.net/ylj__/article/details/164029247>；
- 发布状态、验证日期和公开地址只记录在本文件，不写入可直接发布的文章正文；
- 目前已发布的五篇文章均已收录到上述专栏：模块 0 两篇文章分别于 2026-08-08 发布，模块 1 修订版于 2026-08-19 发布，模块 2 文章于 2026-08-22 发布，模块 3 文章于 2026-08-24 发布。
- 用户确认本轮不回写 CSDN 已发布的前四篇文章，仓库中的文章作为最新复习版本；后续只在公开文章出现命令失效、技术结论错误、安全边界错误或与系列后文严重矛盾时进行回写，不因表达和复习结构优化反复修改旧文。
- 已审核大纲：`docs/学习/大纲/模块0文章大纲.md`；

## 已完成

- 确定项目总体方向；
- 明确工作流平台与 Coding Agent、传统定时任务之间的区别；
- 确定使用单仓库存放代码、学习资料、实验和交接文档；
- 创建本地 Git 仓库；
- 完成本机开发环境初步检查；
- 统一 README 与新窗口启动协议的核心文档阅读顺序；
- 明确缺少当前模块资料时必须在交接中报告“缺失”；
- 完成模块 0 详细计划、零背景术语导览和文档验证脚本；
- 约定可写成博客的学习资料统一放在 `docs/学习/文章/`，发布信息集中记录在 `项目状态.md`；
- 用户审核模块 0 文档并手动提交 Git 基线；
- 用户于 2026-08-08 确认模块 0 完成。
- 用户于 2026-08-14 批准将产品定位收敛为面向 Agent 与确定性程序任务的可靠运行时和调度平台；
- 用户批准将原 10 模块路线调整为模块 0 至模块 7 共 8 个模块，并通过 `ADR-0002` 记录原因和边界；
- 用户确认自然语言转工作流对面试演示和产品理解很重要，批准将其调整为模块 3 的必做最小产品演示能力；
- 用户于 2026-08-18 完成模块 1 的最终审核和手动提交，确认首轮性能数据作为当前基线，暂不设置绝对 CI 性能门禁；
- 用户于 2026-08-19 确认模块 2 需求与设计、实施计划，并开始执行任务 1；任务 1 已完成实现、缺陷修复、独立代码审查和自动化验证，已通过任务级质量门禁。
- 用户于 2026-08-19 将实现阶段的默认审核粒度从“每个任务一次人工审核”调整为“任务级自动质量门禁、模块级一次人工审核”；只有需求、公开接口、数据模型、安全边界、技术选型或外部操作发生重大变化时才增加人工确认点。
- 任务 2 已实现 PostgreSQL 16 迁移、连接池、Workflow/版本事务、Run/TaskRun/Attempt/Event 关系表、增量 Apply、revision 乐观并发控制、事件序号、幂等事务、并发相同 Key 串行化、快照重建、非终态扫描和持久化取消；新增 `DefinitionVersion` 和版本化初始快照工厂，拒绝快照内容与数据库版本不一致及 Attempt 跳号。
- 任务 3 已实现不依赖 HTTP 的 `WorkflowService` 和 `RunService`：创建和版本化工作流先 Compile、事务成功后缓存不可变编译结果；启动 Run 先持久化再异步入队，幂等重放不重复入队；取消先持久化再通知 Engine；Run 摘要、任务详情和事件查询使用有界结果与游标分页，摘要不加载 Attempt 历史。
- 任务 3 测试覆盖非法定义、版本缓存、异步启动顺序、未知版本、幂等重放、取消顺序、分页游标、非法页参数和 PostgreSQL 专用摘要/任务/Attempt/事件查询；真实 PostgreSQL 全仓 `go test ./...`、`go test -race ./...`、`go vet ./...`、两项文档验证和 `git diff --check` 均通过。服务启动扫描和 `Resume` 接入保留在任务 5。
- 任务 4 已实现严格 Bearer Token 解析和 viewer/operator 角色控制、版本化 HTTP API、Workflow/Version/Run/Task/Event/Cancel 的创建、查询、列表和分页路由、统一错误结构、request ID、JSON 请求体限制与未知字段拒绝，并新增 `api/openapi.yaml` 路径契约；认证、HTTP、OpenAPI 定向测试及真实 PostgreSQL 全仓 `go test ./...`、`go test -race ./...`、`go vet ./...`、两项文档验证和 `git diff --check` 均通过。
- 任务 5 已实现环境变量配置校验、无默认 Token、显式 `workload-server migrate up`、只读迁移检查、PostgreSQL Advisory Lock、非终态 Run 启动恢复协调器、恢复失败时取消已启动 Run、结构化请求日志脱敏和恢复完成前拒绝业务请求；新增 `.env.example` 与 `docs/部署/本地开发与配置.md`，说明 Docker 登录不是公开 PostgreSQL 本地开发的前置条件及换电脑流程。配置、协调器、服务器入口、迁移检查和 Advisory Lock 测试通过；实际启动验证中 `live`/`ready` 返回 200，第二实例因锁竞争退出。完整非终态 Run 重启恢复故障实验保留到任务 7 的端到端验收。
- 任务 6 已实现 `pkg/workloadclient` Go SDK，集中处理 Bearer Token、幂等 Key、JSON、分页、Context 取消和统一 `APIError`，不自动重试写请求；`cmd/workload` 保留模块 1 本地 `run/status`，新增控制面 `workflow create`、`workflow add-version`、`run start/status/cancel`，写操作要求显式幂等 Key。SDK、CLI、真实 PostgreSQL 全仓和端到端 HTTP 幂等测试已通过。
- 任务 7 已完成真实 PostgreSQL 端到端夹具、OpenAPI schema、README、换电脑文档、验证报告和模块 2 学习文章；已验证 v1 幂等创建、Run 幂等启动、v2 创建后旧 Run 保持 v1、Task/Attempt/Event 查询、运行中取消，以及“事务提交后尚未入队的 pending Run”被启动恢复扫描并执行成功。
- 模块 2 最终自审修复了 `workload local` 文档与实现不一致、跨包测试共享数据库、日志配置无效、应用输入误报 500、默认 Mock Executor 无脚本必然失败、公开 SDK 泄露 `internal` 类型和 OpenAPI 缺少 JSON schema 等问题；真实 PostgreSQL E2E 已验证临时失败重试成功和下游 Attempt 超时失败。
- 模块 2 最终验收修复进一步完成：Engine 区分业务取消和基础设施中断；Coordinator 统一监督 Execute、Resume 与 Advisory Lock；运行期故障采用安全退出；迁移拒绝未知版本；Workflow、Version、Run 和 Task 使用键集游标，Run 支持 Workflow 与状态过滤；OpenAPI 升级为 3.1 并建立结构化契约测试；新增可复现停库恢复示例和人工演示步骤。
- 模块 3 已完成 Agent Runtime、离线 Mock Model、人工显式启用的 OpenAI-compatible HTTP Model Adapter、只读工作流目录、工具权限、预算、取消、重试、审计、Workflow Draft 生命周期、事实/假设/问题、结构化 Input、草稿校验、内容哈希确认和 Agent CLI。
- 模块 3 已同步 README、OpenAPI、部署与换电脑手册、学习文章、验证报告和固定草稿示例；资源边界明确收窄为任务超时和 Agent Workflow 并发，CPU、内存和临时存储留到模块 6。
- 模块 3 已通过用户于 2026-08-24 的统一人工审核，学习文章已发布至 CSDN 专栏。
- 全仓文档与文章可读性整改已完成：项目状态和路线事实已同步，模块 0 至模块 3 文章已重新检查，模块 3 文章已重构完整流转主线，修改由用户以 `11ca9b9` 提交。

## 正在进行

- 准备进入模块 4 的需求与边界分析，尚未编写模块 4 业务代码。
- 下一步先复核多 Worker、租约、心跳、过期回收、迟到结果和重复执行边界，再向用户提交模块 4 需求与设计方案。

## 历史完成记录（非当前进行中）

- 模块 1 的交付形式、执行对象、定义格式、并发、重试、超时、取消、恢复、校验、状态、持久化、失败传播和验收边界已获得用户确认；
- 性能验证目标确定为单个工作流最多 10,000 个任务、单机最多 1,000 个活跃运行实例；任务使用唯一正式标识 `TaskKey`，校验阶段编译为可共享的内部数组索引；依赖调度使用每次运行独立的未满足依赖计数，恢复时根据完整依赖和任务状态重算；
- 已新增 `docs/计划/模块1需求与设计.md`，设计文档已获用户确认；
- 模块 1 技术学习篇 `docs/学习/文章/模块1-单机可靠工作流内核设计与验证基础.md` 已发布并记录公开地址；
- README、产品说明、架构基线、路线图和验证工具已按新定位统一并通过用户审核；
- ADR-0002、模块 3 路线、产品说明、架构数据流、README、模块 0 更新说明和模块 1 学习文章已同步自然语言工作流为模块 3 必做演示；
- 根据用户审核反馈，模块 0 项目总览源文件已补充路线更新说明，模块 1 设计和学习文章已将 `Mock Agent` 调整为通用的 `Mock Executor` 并同步 8 模块路线；文章已完成审核并发布；
- 根据传统后端公共技术栈参考文章的辩证分析，已补充横向底座原则：模块 2 明确 HTTP/REST/OpenAPI、事务、索引、迁移、幂等和基础运维能力；模块 6 明确 Docker/Compose 先于 Kubernetes；模块 7 增加 CI/CD；Redis、Kafka、对象存储、OAuth/OIDC、gRPC、Elasticsearch 和 API Gateway 保持按需评审；架构基线补充 PostgreSQL 与 MySQL 的选型比较；
- TDD 详细计划已形成并获用户批准，包含公开接口、文件清单、测试夹具、十个可独立审核任务和性能基线步骤；
- 用户确认代码标识符保持英文、关键位置使用符合 Go 工程习惯的中文注释；需求设计和 TDD 计划已加入 Go Doc、关键不变量与边界注释、过期注释检查和人工审核要求，并补充 Action 非空白校验；
- 用户确认 WorkflowDefinition 必须至少包含一个任务；空任务列表作为配置错误拒绝，不定义空工作流立即成功语义，需求设计和任务 1 已加入独立 TDD 步骤；
- 用户确认 Workflow ID 与 TaskKey 使用相同的机器可读格式：长度 1 到 64，只允许小写英文字母、数字、连字符和下划线；未来展示名称使用独立字段，需求设计和任务 1 已加入边界测试；
- 用户确认单个 WorkflowDefinition 最多包含 10,000 个任务；10,001 个任务必须在 Compile 阶段拒绝，需求设计和任务 1 已加入独立上限测试；
- 用户确认单机 1,000 个活跃 Run 只作为性能 Benchmark 规模，不实现第 1,001 个 Run 的容量拒绝；这与 10,000 个任务的 Compile 硬上限明确区分；
- 用户确认 RunID 由系统使用加密随机数生成 16 字节并编码为 32 位十六进制字符串，不接受用户输入，不使用时间戳或自增整数；任务 5 已加入格式测试；
- 审核 TDD 计划时修正了四处边界：状态初始化夹具补齐必填 Action；重复完成保护改为调用无 goroutine 依赖的内部处理函数；恢复阶段先传播失败/取消/跳过终态，避免非拓扑任务顺序遗漏后代；CLI status 测试按 `encoding/json` 默认紧凑输出断言；
- 任务 1 已实现工作流定义、Workflow ID/TaskKey/Action/重试/超时/任务数量校验、深拷贝、TaskKey 到数组下标索引、整数依赖邻接表和 Kahn 环检测；任务 1 测试覆盖合法 DAG、非法定义、边界 ID、重复依赖、默认/非法重试策略、定义隔离和 10,000/10,001 任务规模；
- 任务 2 已实现 Workflow/Task/Attempt 状态常量和合法转换、状态事件序列、Workflow/Task/Attempt 时间字段、RunSnapshot、每 Run 独立依赖计数和状态切片隔离；测试覆盖合法生命周期、非法跳转、事件、终止时间和独立性；
- 任务 3 已实现 Store 接口、每 Run 一个 JSON 文件、同目录临时文件、文件/目录同步、原子 Rename、重复/缺失/损坏/非法路径错误、单进程写互斥和 macOS/Linux 原子替换边界；测试覆盖正常读写、替换失败保护、并发 Create 和平台边界；
- 任务 4 已实现 Clock/RealClock、Executor 协议和 ResultKind、携带 DefinitionID 的 ExecutionRequest、按 `(DefinitionID, TaskKey)` 配置脚本并以 `(RunID, TaskKey)` 独立顺序消费 Step 的 Mock Executor、延迟等待、协作式取消、空 DefinitionID 拒绝和脚本耗尽错误；测试覆盖顺序消费、取消、脚本耗尽、同 Action 不同任务隔离、同任务不同 Run 隔离和跨定义同名任务隔离；
- 任务 5 已实现 Engine、EngineOptions、加密随机 32 位十六进制 RunID、初始快照持久化、进程内编译结果缓存与从快照重新编译回退、成功路径 DAG 调度、DefinitionID 传播、并发上限、单一完成消息通道、任务 running 快照先保存后启动 Executor、重复完成保护和最终成功状态；测试覆盖 DAG 依赖解锁、DefinitionID 传播、并发上限、随机 RunID、保存失败不启动 Executor 和重复完成不重复解锁；
- 任务 6 已实现临时失败与永久失败分类、固定间隔重试、Attempt 独立超时、超时先取消执行器、迟到结果按任务下标和 Attempt 编号隔离、重试耗尽失败、后代跳过、独立分支继续运行和最终 WorkflowFailed；测试覆盖错误结果保存、超时重试、失败沿多层 DAG 传播、运行中并行重试、失败/超时状态保存失败不重试，以及 Run 结束后迟到完成和超时通知不阻塞发送 goroutine；
- 用户审核任务 6 时指出仅有函数声明注释不足以理解复杂实现；现已为模块 1 的抽象结构体字段补充数据语义、索引口径、生命周期和锁保护说明，并在长函数内部按关键阶段解释状态所有权、持久化顺序、异步事件汇合和失败边界；需求设计与详细计划已同步为后续代码的固定要求；
- 任务 7 已实现 `Cancel(context.Context, RunID)`、活动 Run 请求式取消、非活动 pending/running/waiting_retry 快照取消、父 Context 主动取消与截止时间取消、`ResultCanceled` 处理、取消终态清理保存、系统错误停止调度和其他活动 Attempt 取消；Execute 在加载前注册 active，并在启动 Executor 和解锁下游的关键保存边界复查取消，防止旧快照覆盖取消终态、取消后启动任务或制造未执行 Attempt；
- 任务 7 测试覆盖运行中与等待重试取消、非活动快照、终态与重复取消幂等、未知 RunID、取消保存失败保留旧快照、父 Context 取消、Execute/Cancel 注册竞态、保存期间取消、成功保存失败不解锁下游、Executor canceled 结果和系统错误取消其他 Attempt；
- 任务 8 已确认采用 Execute/Resume 入口分离、内部 `executeSnapshot` 共享的方案；Resume 在加载快照前注册 active，running Attempt 记为 interrupted 并计入最大次数，有剩余次数时立即重试；已有 waiting_retry 保留原 ReadyAt，到期后立即重试；终态 Workflow 幂等返回且不重新编译 Definition；
- 任务 8 步骤 1 已提取 `registerActiveRun` 和 `executeSnapshot`，Execute 行为保持不变；步骤 2 已在 `workflow/recovery_test.go` 增加 running Attempt 中断重试、未来 `ReadyAt` 保留和依赖计数重算测试，步骤 3 已确认测试曾因 `Resume` 缺失而按预期编译失败；
- 任务 8 步骤 4 已实现 `Resume` 入口和 `prepareForResume`：终态幂等返回，非终态从持久化定义重新编译；running Attempt 标记为 interrupted 并按剩余次数立即重试或失败；依赖计数从 DAG 和任务事实重算；已有 waiting_retry 保留 ReadyAt，并在共享调度核心启动前恢复计时器；恢复副本保存成功后才缓存编译结果和启动 Executor；
- 任务 8 步骤 5 已增加 ReadyAt 已过期时无需推进时钟即可重试，以及任务数组不是拓扑顺序时仍完整跳过失败后代的测试；受控变异分别触发“Executor 未启动”和“孙任务未跳过”的预期失败，恢复代码后相关测试连续 20 次通过；
- 任务 8 步骤 6 已覆盖至少执行一次、成功任务不重跑、中断 Attempt 耗尽次数、succeeded/failed/canceled 终态原样返回、定义缺失或编译失败、非法 WorkflowStatus、任务数量与 TaskKey 不匹配、running 缺少 Attempt、waiting_retry 缺少 ReadyAt、恢复 Save 失败不执行、Resume/Execute 并发互斥和恢复期间父 Context 取消；所有新增测试在现有实现上通过；
- 任务 8 最终代码审查发现并修复快照 RunID 不匹配导致 FileStore 跨 Run 写入、Version/DefinitionID 混用、Attempt 历史突破 MaxAttempts、非 waiting_retry 状态残留 ReadyAt 四类风险；补齐终态身份校验、Execute/Resume 双向互斥、Resume/Resume 互斥、失败释放 active、保存失败不缓存 compiled 和合法恢复状态正向测试；
- 恢复失败传播已从“每个失败源分别遍历”调整为一次多源 DAG 遍历，避免 10,000 任务场景最坏退化到 O(V²)，保持 O(V + E)；独立复审最终无 Critical、无 Important；
- 任务 9 已实现 `workload run <workflow.json>` 和 `workload status <run-id>`，默认数据目录为 `.workload/runs`，可通过 `WORKLOAD_DATA_DIR` 覆盖；run 使用严格 JSON 解码、Compile、FileStore 和 Mock Executor 前台执行，status 只加载并输出 WorkflowRun JSON，不调用 Engine 或保存快照；
- 任务 9 使用私有 Executor 工厂可靠模拟运行中取消，不改变外部命令或公开 Go API；测试覆盖成功运行与查询、参数错误、错误/未知字段/多值 JSON、未知 RunID、status 字节级只读、运行中 Attempt 与未启动任务取消，以及结果输出失败；
- 已新增 `examples/document-pipeline.json`，人工演示确认 read、clean、summarize 按依赖顺序全部成功；`.gitignore` 已有 `.workload/` 规则并经 `git check-ignore` 验证。代码自审发现并通过 RED → GREEN 修复最终输出失败仍返回 0 的问题；
- 任务 10 已新增内核和 FileStore Benchmark，覆盖 10,000/10,001 任务编译、1,000 Run 共享/独立定义、编译缓存命中、9,999 条依赖解锁、10,000 任务恢复预处理，以及 100/10,000 任务且带等量事件的 JSON 快照 Save/Load；
- 五轮基准运行环境为 macOS 26.1、Apple M4、10 个逻辑 CPU、32 GiB、Go 1.26.2；10,000 任务编译均值 1.841 ms，1,000 Run 共享定义均值 1.376 ms/9.27 MB，分别编译均值 17.662 ms/31.50 MB，10,000 任务 FileStore Save/Load 均值 26.559/29.887 ms；完整原始输出和限制见 `docs/实验/模块1性能基线.md`；
- 基准支持共享不可变编译结果的设计；当前主要瓶颈是完整 JSON 快照的编解码、分配和同步写入，以及大规模状态事件产生的分配。超过 10,000 任务的定义当前在任务数量检查前进行深拷贝，是进入不可信网络输入阶段前的优化候选；本轮只记录证据，未在用户确认前设置绝对性能承诺或 CI 门禁；
- README 已加入真实快速开始、status 查询、数据目录、Mock Executor、FileStore、macOS/Linux、至少执行一次、规模口径、性能报告和未实现能力边界；模块 1 需求覆盖与中文注释已逐项复核；

## 已知文档差异

- 模块 0 项目总览源文件已经增加路线更新说明，但已发布的 CSDN 正文尚未同步；是否更新公开页面，需要在路线调整审核后由用户决定；
- 模块 1 学习文章已同步产品定位、Mock Executor 和新模块顺序，完成发布前内容检查并发布到 CSDN；
- `docs/计划/模块0详细计划.md` 和早期 `docs/superpowers/` 文档属于历史过程记录，可能保留旧表述；当前决策以 `ADR-0002`、产品说明、架构基线和项目路线图为准。

## 当前环境

已检测到：

- Git 2.50.1；
- Go 1.26.2；
- Python 3.9.6；
- Node.js 24.13.1；
- Homebrew；
- 可用磁盘空间约 254 GiB。

尚未检测到：

- Rust 与 Cargo；
- kubectl、kind、k3d 或 minikube；
- 本地 Redis、Kafka；
- 项目所需 Python 第三方包。

当前结论：

- 模块 0 和单机工作流内核可以仅使用 Go 与 Mock Executor 开发；
- 单元测试和大部分调度测试不依赖真实模型 API；
- Docker Desktop、Docker Engine、Docker Compose 和 PostgreSQL 16 容器已经可用；当前 Compose 只提供数据库，不代表任务容器化；
- Kubernetes 是已批准的学习和验证目标，但安排在核心工作流跑通后；
- Kubernetes 阶段需要本地轻量集群工具；
- Rust 仅在进入受限执行节点模块前安装；
- 不在需求尚未出现前批量安装依赖。

## 低成本验证原则

- 默认使用 Mock Executor，不消耗模型 API 额度；
- 真实模型只用于少量人工触发的端到端测试；
- 一个合法 API Key 可以承载多个逻辑 Agent，但必须遵守供应商速率限制和使用条款；
- 不自动化未开放的网页界面，不绕过产品限制；
- 所有基础设施优先在本机运行；
- 初期不依赖付费云服务器、GPU 或多个模型账号；
- 测试需要能够在没有外部网络的情况下运行；
- 外部依赖必须通过接口隔离并提供 Mock 或 Fake 实现。

## 测试状态

- 业务代码：任务 1 的 `workflow/definition.go`、`workflow/compiler.go`，任务 2 的 `workflow/state.go`、`workflow/run.go`，任务 3 的 `workflow/store.go`、`workflow/filestore/store.go`，任务 4 的 `workflow/clock.go`、`workflow/executor.go`、`workflow/mockexec/executor.go`，任务 5 至任务 8 的 `workflow/engine.go`、`workflow/recovery.go`，任务 9 的 `cmd/workload/main.go` 和 `examples/document-pipeline.json`；任务 10 不修改生产逻辑，新增 Benchmark、性能报告和 README；
- 业务测试：`workflow/compiler_test.go`、`workflow/state_test.go`、`workflow/filestore/store_test.go`、`workflow/mockexec/executor_test.go`、`workflow/engine_test.go`、`workflow/recovery_test.go`、`workflow/test_helpers_test.go`、`cmd/workload/main_test.go`；
- 已运行 `go test ./...`、`go vet ./...`、`go doc ./workflow`，均通过；默认 Go 缓存曾在中断命令后挂起，使用临时 `GOCACHE` 的 `go vet ./...` 已通过，属于本机工具缓存状态而非代码诊断；
- 已运行文档检查：`bash scripts/test-verify-docs.sh` 的 8/8 场景通过；`bash scripts/verify-docs.sh` 检查 14 个必需文件、21 个本地链接、本机绝对路径和 Markdown 尾随空白并通过；`git diff --check` 通过；
- 已运行模块 1 性能基准：`go test ./workflow/... -run '^$' -bench . -benchmem -count=5`，完整原始结果见 `docs/实验/模块1性能基线.md`；新增文档后本地链接数量为 21；
- 当前结论：模块 3 的代码、测试、文档、人工审核、Git 提交和学习文章发布已完成；全仓文章可读性整改也已完成，下一步进入模块 4 需求与边界分析。
- 模块 2 任务 1 已新增增量 `Persistence`、最小 `RunStore`、行级 `ChangeSet`、Run revision、事件序号和持久化取消请求字段；模块 1 原有 `Store.Create/Save/Load` 接口保持兼容。Engine 通过 `Apply` 提交，旧 Store 由单写者适配器回退到完整快照；FileStore 已适配 `Apply`、非终态扫描和幂等取消请求。
- 任务 1 测试覆盖 revision 冲突与内存/持久化版本一致性、TaskRun 依赖计数、Attempt insert/update、非法 ChangeSet、连续事件序号、旧快照事件兼容、RunID 损坏、FileStore 非终态扫描、重复/并发取消，以及取消请求与任务完成、Execute、Resume 的竞争窗口；`go test ./...`、`go test -race ./...`、`go vet ./...`、`go doc ./workflow`、两项文档验证和 `git diff --check` 均通过。
- 任务 2 测试覆盖空库迁移、Workflow v1/v2 创建和读取、事务回滚、revision 冲突、关系表快照重建、Run 幂等重放和冲突、相同 Key 并发创建、非终态扫描、重复取消、定义版本一致性和 Attempt 连续编号；带真实 PostgreSQL 的 `go test ./...`、`go test -race ./...`、`go vet ./...`、两项文档验证和 `git diff --check` 均通过。
- 任务 3 至任务 6 已完成应用服务、HTTP/REST/OpenAPI、Bearer Token、配置迁移、结构化日志、健康检查、恢复协调、Go SDK、`workload local` 与控制面 CLI；公开 SDK 使用自身 DTO，不依赖仓库外不可导入的 `internal` 类型。
- 任务 7 的真实 PostgreSQL E2E 已通过版本、幂等、查询、临时失败重试、Attempt 超时、取消和提交后未入队恢复路径；集成测试使用独立临时数据库隔离业务表与 Advisory Lock。
- 模块 2 控制面性能基线使用 1 个任务、每轮 100 次操作运行 5 轮：Run 串行查询平均 0.281 ms/op，并行查询总耗时折算平均 0.081 ms/op，创建 pending Run 平均 1.234 ms/op；原始输出、输入和限制见 `docs/实验/模块2性能基线.md`，不作为生产性能承诺。
- 2026-08-22 最新代码重新通过带真实 PostgreSQL 的 `go test ./... -count=1`、`go test -race ./... -count=1`、`go vet ./...`、8/8 文档验证场景、14 个必需文件与 29 个本地链接检查和 `git diff --check`；正常 CLI 服务、第二实例锁竞争和真实停库恢复人工演示通过，恢复 Run 的 Attempt 1 为 `interrupted`、Attempt 2 为 `succeeded`，Run 最终为 `succeeded`。
- 2026-08-24 模块 3 最新验证通过：`go test ./... -count=1`、`go test -race ./... -count=1`、`go vet ./...`、真实 PostgreSQL `internal/postgres` 与 `internal/e2e` 集成测试、8/8 文档验证场景、14 个必需文件与 33 个本地链接检查、`git diff --check`；Mock CLI 确认成功，错误哈希返回 `draft_changed`。

## 候选想法

以下内容讨论过，但尚未批准为正式功能：

- 同时适配多个商业 Coding Agent；
- 企业级多租户与计费；
- 托管 SaaS 版本；
- 自动生成并提交 Pull Request；
- 跨区域调度；
- 微虚拟机级别的安全沙箱。

自然语言生成工作流草稿不单独占用模块，但已确定为模块 3 的必做最小产品演示；模块 7 负责将它接入控制台。它不作为模块 1 或模块 2 的前置条件，必须经过校验和用户确认后才能进入可靠执行内核。

候选想法不得直接进入开发计划，必须先分析价值、成本和范围影响，再由用户批准。

## 变更规则

- 概念问题只用于解释，不自动修改项目方向；
- 新想法先进入“候选想法”；
- 架构或范围调整必须说明原因、收益、成本和受影响模块；
- 用户明确批准后才能移动到“已批准范围”；
- 每次范围变化都要增加架构决策记录。
- `docs/部署/本地开发与配置.md` 是“项目本地运行、演示与换电脑手册”的固定路径，作为新电脑恢复环境和面试前跑通项目的唯一详细入口，不是模块 2 的一次性交付文档；
- 每个后续模块在模块级审核前都要检查并按实际能力更新该手册，包括依赖、配置、运行架构、启动和停止顺序、健康检查、故障排查、当前推荐演示、换电脑清单和验证日期；
- 新演示入口取代旧入口时，应将旧入口标记为兼容或历史路径。只有在目标 Mac mini M4 上重新跑通完整流程后，才能把新增流程写成已验证能力；
- README 只保留最短快速开始，详细安装、运行和演示步骤统一链接到上述手册，避免两处维护完整命令造成不一致。

## 下一步

1. 执行新窗口启动协议要求的仓库事实复核，定位模块 4 的已有路线与架构边界；
2. 形成模块 4 的需求与设计方案，向用户解释接口、数据模型、故障语义、技术选型和未选方案；
3. 用户确认需求与设计后，编写模块 4 详细实施计划；
4. 用户批准详细计划后才开始业务代码实现。

## 新会话同步流程

详细流程见 [`docs/新窗口启动协议.md`](docs/新窗口启动协议.md)。核心步骤如下：

1. 完整阅读本文件；
2. 阅读当前模块计划和相关架构/学习资料；
3. 执行 `git status --short --branch`；
4. 阅读最近的 Git 提交记录；
5. 检查相关代码和测试；
6. 运行当前模块的验证命令；
7. 核对文档描述和仓库事实是否一致；
8. 先向用户汇报理解到的当前状态、已完成内容和下一步，不要直接写代码。

## 最近交接

- 日期：2026-08-24；
- 分支：`main`；
- 当前模块：模块 3 已全部收尾；准备进入模块 4 需求与边界分析，尚未开始实现。
- 最近提交：`11ca9b9 重构全系列学习文章并完善项目文档证据链`；
- 本次完成：全仓文档与文章可读性整改已审核、验证并由用户手动提交；模块 3 学习文章已发布至 CSDN，公开地址已记录。
- 尚未完成：模块 4 需求与设计、学习资料、详细实施计划和业务实现。
- 当前阻塞或待确认：无技术阻塞；模块 4 开始前需要先向用户提交需求与边界方案并获得确认。
- 验证命令及结果：全仓 `go test ./...`、`go test -race -count=1 ./...`、`go vet ./...`、真实 PostgreSQL `internal/postgres`/`internal/e2e`、8/8 文档场景、14 个必需文件、39 个本地链接和 `git diff --check` 均通过；正确哈希确认成功，错误哈希返回 `draft_changed`。
- 下一窗口先读或先做：先执行新窗口启动协议，再阅读模块 4 路线、架构基线、模块 3 故障边界和学习资料规范；形成需求与设计方案并等待用户确认，不直接写代码。
- 模块 2 改动已由用户提交；后续模块仍由用户在统一审核后手动提交，Codex 不自动提交。
