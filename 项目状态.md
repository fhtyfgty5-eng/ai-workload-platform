# Project Context

## 文件作用

这是跨窗口、跨会话同步项目状态的唯一入口。新会话开始后，必须先阅读本文件，再检查 Git 状态、最近提交、当前阶段计划和相关测试，不能仅凭本文件假设代码现状。

## 项目目标

构建一个面向 AI 和数据任务的可靠工作流编排与执行平台。

平台接收一个由多个步骤组成的工作流，按照步骤之间的依赖关系调度任务，并负责记录状态、分配执行节点、处理失败、限制资源以及展示执行过程。

一句话解释：

> 让一项跨多个步骤、可能持续较长时间、执行过程中可能失败的后台工作，最终能够被可靠地完成。

## 已批准范围

项目主干包括：

- 工作流定义与依赖关系；
- 单任务和多任务执行；
- 单 Agent 和多 Agent 执行模式；
- 使用自然语言生成工作流草稿，并经过校验和用户确认后执行；
- 持久化任务状态；
- 重试、超时、取消、恢复和故障转移；
- 执行节点管理和资源控制；
- 日志、指标、调用链和审计；
- Agent 受限执行场景；
- 搜索或推荐数据处理流水线场景。

产品主线不是“强制多个 Agent 并发”，而是根据任务特征选择单 Agent、多 Agent 或普通程序任务，并对整个执行过程进行可靠管理。

## 非目标范围

当前不以以下目标为项目承诺：

- 替代所有程序员或完全自动完成软件项目；
- 支撑亿级用户或大规模生产集群；
- 自研数据库、消息队列、容器运行时或 Kubernetes；项目会使用并学习 Kubernetes，但不重复实现 Kubernetes 本身；
- 自研基础大模型；
- 安全运行任意恶意代码；
- 完整实现商业广告竞价或复杂推荐算法；
- 保证项目能够商业化或达到特定薪资水平。

## 当前阶段

- 阶段：模块 0，项目定义与交接基线。
- 状态：两篇模块 0 文章已通过用户内容审核、官方事实核对和发布前检查，均达到“可发布”状态。
- 当前目标：等待用户手动提交并将两篇文章发布到 CSDN；发布地址记录完成前不进入模块 1。
- 当前没有业务代码。

## 博客专栏与文章状态

- 专栏名称：从 0 构建 AI Workload Platform：可靠工作流工程实践；
- 项目总览篇：`从 0 构建 AI Workload Platform（一）：为什么做、做什么、与现有产品有什么不同`，状态为“可发布”，正文位于 `docs/学习/文章/模块0-项目总览.md`；用户内容审核、结构检查、内部内容检查、官方资料核对和公开可读性检查均于 2026-08-08 通过；
- 技术学习篇：`从 0 构建 AI Workload Platform（二）：工作流、DAG、状态机与可靠执行基础`，状态为“可发布”，正文位于 `docs/学习/文章/模块0-工作流-DAG-状态机与可靠执行基础.md`；用户内容审核、技术深度检查、内部内容检查、官方资料核对和公开可读性检查均于 2026-08-08 通过；文章明确说明尚无业务代码，实验内容为后续验证计划；
- 发布状态、验证日期和公开地址只记录在本文件，不写入可直接发布的文章正文；
- 两篇文章均可发布但尚未发布，目前没有 CSDN 公开地址。
- 已审核大纲：`docs/学习/大纲/模块0文章大纲.md`；

## 已完成

- 确定项目总体方向；
- 明确工作流平台与 Coding Agent、传统定时任务之间的区别；
- 确定使用单仓库存放代码、学习资料、实验和交接文档；
- 创建本地 Git 仓库；
- 完成本机开发环境初步检查；
- 统一 README 与新窗口启动协议的核心文档阅读顺序；
- 明确缺少当前模块资料时必须在交接中报告“缺失”；
- 完成模块 0 详细计划、零背景术语导览和文档验证脚本；
- 约定可写成博客的学习资料统一放在 `docs/学习/文章/`，发布信息集中记录在 `项目状态.md`；
- 用户审核模块 0 文档并手动提交 Git 基线。

## 正在进行

- 已固化项目总览篇、技术学习篇和模块实践篇的职责、正文边界和分类发布证据；
- 模块 0 原单篇文章已拆分为项目总览篇和技术学习篇，两篇均达到“可发布”状态；
- 已完成两篇文章大纲；
- 已完成项目总览篇正文和发布前检查；
- 已根据大纲整理技术学习篇正文，将文件更名为 `docs/学习/文章/模块0-工作流-DAG-状态机与可靠执行基础.md`，并完成发布前检查；
- 用户已阅读并认可两篇正文；
- 两篇文章已补齐目录、章节编号和官方参考资料，并通过内部内容与敏感信息检查；
- 在两篇大纲和后续正文获得用户确认前，不进入模块 1 设计或业务代码开发。

## 当前环境

已检测到：

- Git 2.50.1；
- Go 1.26.2；
- Python 3.9.6；
- Node.js 24.13.1；
- Homebrew；
- 可用磁盘空间约 254 GiB。

尚未检测到：

- Rust 与 Cargo；
- Docker、Podman 或其他容器运行时；
- kubectl、kind、k3d 或 minikube；
- 本地 PostgreSQL、Redis、Kafka；
- 项目所需 Python 第三方包。

当前结论：

- 模块 0 和单机工作流内核可以仅使用 Go 与 Mock Agent 开发；
- 单元测试和大部分调度测试不依赖真实模型 API；
- 容器沙箱阶段需要安装一个本地容器运行时；
- Kubernetes 是已批准的学习和验证目标，但安排在核心工作流跑通后；
- Kubernetes 阶段需要本地轻量集群工具；
- Rust 仅在进入受限执行节点模块前安装；
- 不在需求尚未出现前批量安装依赖。

## 低成本验证原则

- 默认使用 Mock Agent，不消耗模型 API 额度；
- 真实模型只用于少量人工触发的端到端测试；
- 一个合法 API Key 可以承载多个逻辑 Agent，但必须遵守供应商速率限制和使用条款；
- 不自动化未开放的网页界面，不绕过产品限制；
- 所有基础设施优先在本机运行；
- 初期不依赖付费云服务器、GPU 或多个模型账号；
- 测试需要能够在没有外部网络的情况下运行；
- 外部依赖必须通过接口隔离并提供 Mock 或 Fake 实现。

## 测试状态

- 业务代码：尚无；
- 业务测试：尚无；
- 已运行文档检查：`bash scripts/test-verify-docs.sh` 的 4/4 场景通过；`bash scripts/verify-docs.sh` 检查 11 个必需文件和 10 个本地链接并通过；`git diff --check` 通过；
- 业务测试仍不存在；上述命令只验证文档基线和验证脚本行为；
- 当前结论：不能把文档检查结果表述为业务测试通过。

## 候选想法

以下内容讨论过，但尚未批准为正式功能：

- 同时适配多个商业 Coding Agent；
- 企业级多租户与计费；
- 托管 SaaS 版本；
- 自动生成并提交 Pull Request；
- 跨区域调度；
- 微虚拟机级别的安全沙箱。

自然语言生成工作流草稿、依赖/参数/权限/资源校验以及用户确认已经纳入已批准范围，但具体实现方式仍需在模块计划中确定。

候选想法不得直接进入开发计划，必须先分析价值、成本和范围影响，再由用户批准。

## 变更规则

- 概念问题只用于解释，不自动修改项目方向；
- 新想法先进入“候选想法”；
- 架构或范围调整必须说明原因、收益、成本和受影响模块；
- 用户明确批准后才能移动到“已批准范围”；
- 每次范围变化都要增加架构决策记录。

## 下一步

1. 用户人工审核完整 Git 差异并手动提交；
2. 用户将两篇文章发布到 CSDN，并提供公开地址；
3. 在本文件记录公开地址和“已发布”状态；
4. 用户确认模块 0 完成并另行批准后，才能进入模块 1；当前不进入模块 1。

## 新会话同步流程

详细流程见 [`docs/新窗口启动协议.md`](docs/新窗口启动协议.md)。核心步骤如下：

1. 完整阅读本文件；
2. 阅读当前模块计划和相关架构/学习资料；
3. 执行 `git status --short --branch`；
4. 阅读最近的 Git 提交记录；
5. 检查相关代码和测试；
6. 运行当前模块的验证命令；
7. 核对文档描述和仓库事实是否一致；
8. 先向用户汇报理解到的当前状态、已完成内容和下一步，不要直接写代码。

## 最近交接

- 日期：2026-08-08；
- 分支：`main`；
- 当前模块：模块 0，博客规则完善和文章拆分准备阶段；
- 最近提交：`d0c7688 完善模块 0 文档基线、博客资料规范和验证脚本`；
- 本次完成：完成两篇文章、用户内容审核、文件更名、目录与章节修正、内部内容检查，使用 Airflow、Prefect、Temporal、Argo Workflows、Kubernetes 和 LangGraph 官方文档核对产品定位，并按文章类型细化发布证据；两篇文章均达到“可发布”状态；
- 尚未完成：用户手动提交、CSDN 发布和公开地址记录；
- 当前阻塞或待确认：无内容或技术阻塞；等待用户人工审核完整差异、手动提交并发布文章；
- 验证命令及结果：`bash scripts/test-verify-docs.sh` 通过（4/4）；`bash scripts/verify-docs.sh` 通过（11 个必需文件、10 个本地链接）；`git diff --check` 通过；
- 下一窗口先读或先做：先阅读本文件，确认用户是否已手动提交和发布两篇文章；获得公开地址后更新发布状态，不进入模块 1；
- 无业务代码和业务测试。
