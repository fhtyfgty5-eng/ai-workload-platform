# 安全策略

## 支持范围

安全问题请优先针对默认分支的最新代码提交报告。历史实验分支和本地未提交文件不属于公开运行服务。

## 报告方式

请通过 GitHub Security Advisories 创建私密报告，不要在公开 Issue 中发布可利用细节、Token、密码、个人信息或业务数据。报告应包含受影响版本、复现步骤、影响范围和建议修复方式。

## 已知边界

- `.env.local`、Token、密码和模型 API Key 不得提交。
- 默认 Mock Model 不访问外部网络；真实模型配置由服务端管理。
- 容器和 Kubernetes 执行器提供限制，但不能单独视为绝对安全隔离。
- 租约可以拒绝迟到结果，不能撤销已经发生的外部副作用。
