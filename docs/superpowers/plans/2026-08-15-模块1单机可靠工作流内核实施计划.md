# 模块 1 单机可靠工作流内核实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Do not use subagents unless the user explicitly requests them. Every task ends at a user review gate; Codex must not stage or commit changes.

**Goal:** 使用 Go 标准库实现可在单机运行、测试、恢复和基准验证的可靠工作流内核，并提供最小 CLI 演示。

**Architecture:** 公开的 `workflow` 包定义工作流模型、编译结果、状态机、执行器、存储、时钟和 Engine 接口；`workflow/filestore` 与 `workflow/mockexec` 分别实现 JSON 文件存储和可编程模拟执行器；`cmd/workload` 只组合这些公开接口。Engine 在单个运行实例内串行修改状态和持久化，Executor 可以并发执行，只有状态保存成功后才解锁下游任务。

**Tech Stack:** Go 1.26、Go 标准库、JSON、`testing`、Go Benchmark、Bash 文档验证脚本。

## 全局约束

- Go module 路径固定为 `github.com/fhtyfgty5-eng/ai-workload-platform`，`go.mod` 记录 `go 1.26.0`。
- 模块 1 不增加第三方依赖，不访问外部网络，不调用真实模型，不执行本机命令或容器。
- Workflow ID 长 1 到 64 个字符，只允许小写英文字母、数字、连字符和下划线；它是机器标识，未来展示名称使用独立字段。
- RunID 由系统使用 `crypto/rand` 生成 16 字节并编码为 32 位十六进制字符串，不接受用户输入，不使用时间戳或自增整数。
- 每个 WorkflowDefinition 必须至少包含一个任务；空任务列表作为配置错误拒绝，不创建“立即成功”的空运行实例。
- 每个 WorkflowDefinition 最多包含 10,000 个任务；10,001 个任务在 Compile 阶段拒绝，10,000 个任务是硬输入边界而不只是 Benchmark 规模。
- TaskKey 长 1 到 64 个字符，只允许小写英文字母、数字、连字符和下划线；同一工作流内唯一。
- `Action` 表示 Executor 的动作查找键，必须至少包含一个非空白字符；模块 1 不把它解释为 Shell 命令、脚本或任意代码。
- `concurrency` 必须大于零；任务省略重试配置时 `max_attempts` 默认为 `1`，`interval_ms` 默认为 `0`；`timeout_ms` 必须大于零。
- 只支持固定重试间隔；临时失败、超时和进程中断可重试，永久失败和用户取消不重试。
- 文件存储以“每个 Run 一个 JSON 文件”保存完整快照、Attempt 历史和状态事件；原子替换只承诺 macOS 与 Linux，同一目录、同一文件系统。
- 模块 1 的 CLI 只有 `workload run <workflow.json>` 和 `workload status <run-id>`；默认数据目录为 `.workload/runs`，可由 `WORKLOAD_DATA_DIR` 覆盖。
- 目标规模是单个工作流 10,000 个任务、单机 1,000 个活跃 Run；10,000 个任务是 Compile 硬输入上限，1,000 个活跃 Run 只是 Benchmark 规模，不实现第 1,001 个 Run 的容量拒绝；先记录数据，再与用户确认性能阈值。
- 每个行为严格执行 RED、确认预期失败、GREEN、全量回归、REFACTOR；未观察到正确失败不得写生产代码。
- 代码标识符使用英文，代码注释使用中文；导出标识符使用以自身名称开头的 Go Doc 注释，关键算法、状态不变量、并发所有权、持久化顺序、恢复语义、性能和安全边界解释“为什么”与不可破坏的条件，不逐行复述自解释代码。
- 测试注释只解释不直观的场景构造、并发同步和故障注入；每个任务人工审核时同步检查注释准确性，不保留与实现不一致的过期注释，模块 1 不为此增加第三方 lint 依赖。
- 每个任务完成后暂停供用户审核；用户自行提交 Git，Codex 只在需要时提供中文 commit 描述。
- 所有业务测试通过后仍需运行 `go test ./...`、`go vet ./...`、`bash scripts/test-verify-docs.sh`、`bash scripts/verify-docs.sh` 和 `git diff --check`。

## 文件结构

| 文件 | 职责 |
| --- | --- |
| `go.mod` | Go module 和最低 Go 版本 |
| `.gitignore` | 忽略 CLI 生成的 `.workload/` 数据 |
| `workflow/definition.go` | WorkflowDefinition、TaskDefinition、RetryPolicy、TaskKey |
| `workflow/compiler.go` | 定义校验、TaskKey 索引、邻接表、环检测 |
| `workflow/compiler_test.go` | 编译正确性、错误输入和 10,000 任务测试 |
| `workflow/state.go` | Workflow、Task、Attempt 状态和合法转换 |
| `workflow/run.go` | WorkflowRun、TaskRun、Attempt、StateEvent、RunSnapshot |
| `workflow/state_test.go` | 状态转换、运行初始化和依赖计数测试 |
| `workflow/store.go` | Store 接口和存储错误 |
| `workflow/filestore/store.go` | JSON 快照、临时文件、同步和原子替换 |
| `workflow/filestore/store_test.go` | 创建、覆盖、损坏文件、原子替换边界测试 |
| `workflow/clock.go` | Clock 接口和 RealClock |
| `workflow/executor.go` | Executor 接口、请求和结构化结果 |
| `workflow/mockexec/executor.go` | 按 Action 配置结果序列、延迟和取消的 Mock Executor |
| `workflow/mockexec/executor_test.go` | Mock Executor 行为测试 |
| `workflow/engine.go` | CreateRun、Execute、GetRun、Cancel 和串行状态更新 |
| `workflow/test_helpers_test.go` | Engine、恢复和 Benchmark 共用的内存 Store、可控 Clock、测试 Executor 与工作流工厂 |
| `workflow/recovery.go` | Resume、执行中 Attempt 中断处理和依赖计数重算 |
| `workflow/engine_test.go` | 调度、并发、重试、超时、取消、失败传播和存储失败 |
| `workflow/recovery_test.go` | 进程重启恢复和至少执行一次边界 |
| `workflow/benchmark_test.go` | 编译、调度、共享定义和快照性能基准 |
| `cmd/workload/main.go` | `run`、`status`、信号取消和终端输出 |
| `cmd/workload/main_test.go` | CLI 参数、运行、状态和 Ctrl+C 等价取消测试 |
| `examples/document-pipeline.json` | 可公开运行的最小工作流示例 |
| `docs/实验/模块1性能基线.md` | 环境、命令、原始基准结果和待确认阈值 |
| `README.md` | 构建、运行示例、当前能力和限制 |
| `项目状态.md` | 实现阶段、验证事实和下一步 |

---

### 任务 1：初始化 Go module，定义并编译工作流

**文件：**

- 新建：`go.mod`
- 新建：`.gitignore`
- 新建：`workflow/definition.go`
- 新建：`workflow/compiler.go`
- 新建：`workflow/compiler_test.go`

**接口：**

- 输入：`WorkflowDefinition`，包含 ID、并发上限和 TaskDefinition 数组。
- 输出：`Compile(WorkflowDefinition) (*CompiledWorkflow, error)`。
- 后续依赖：Engine 使用 `CompiledWorkflow` 的不可变定义、TaskKey 索引、直接下游邻接表和依赖数组。

- [ ] **步骤 1：建立 module 并先写编译失败测试**

```go
// go.mod
module github.com/fhtyfgty5-eng/ai-workload-platform

go 1.26.0
```

```go
// workflow/compiler_test.go
package workflow

import (
	"fmt"
	"testing"
)

func TestCompileBuildsIndexAndDependencies(t *testing.T) {
	def := WorkflowDefinition{
		ID: "document-pipeline",
		Concurrency: 2,
		Tasks: []TaskDefinition{
			{Key: "read", Action: "mock-read", TimeoutMillis: 1000},
			{Key: "clean", Action: "mock-clean", DependsOn: []TaskKey{"read"}, TimeoutMillis: 1000},
			{Key: "publish", Action: "mock-publish", DependsOn: []TaskKey{"clean"}, TimeoutMillis: 1000},
		},
	}

	compiled, err := Compile(def)
	if err != nil {
		t.Fatalf("Compile() error = %v", err)
	}
	if got := compiled.index["clean"]; got != 1 {
		t.Fatalf("clean index = %d, want 1", got)
	}
	if got := compiled.successors[0]; len(got) != 1 || got[0] != 1 {
		t.Fatalf("read successors = %v, want [1]", got)
	}
	if got := compiled.dependencies[2]; len(got) != 1 || got[0] != 1 {
		t.Fatalf("publish dependencies = %v, want [1]", got)
	}
}

func TestCompileRejectsInvalidDefinitions(t *testing.T) {
	tests := []struct {
		name string
		def  WorkflowDefinition
	}{
		{name: "empty workflow id", def: WorkflowDefinition{Concurrency: 1}},
		{name: "invalid workflow id", def: WorkflowDefinition{ID: "Bad ID", Concurrency: 1}},
		{name: "zero concurrency", def: WorkflowDefinition{ID: "w"}},
		{name: "empty tasks", def: WorkflowDefinition{ID: "w", Concurrency: 1}},
		{name: "invalid key", def: WorkflowDefinition{ID: "w", Concurrency: 1, Tasks: []TaskDefinition{{Key: "Bad Key", Action: "run", TimeoutMillis: 1}}}},
		{name: "duplicate key", def: WorkflowDefinition{ID: "w", Concurrency: 1, Tasks: []TaskDefinition{{Key: "a", Action: "first", TimeoutMillis: 1}, {Key: "a", Action: "second", TimeoutMillis: 1}}}},
		{name: "blank action", def: WorkflowDefinition{ID: "w", Concurrency: 1, Tasks: []TaskDefinition{{Key: "a", Action: "  ", TimeoutMillis: 1}}}},
		{name: "missing dependency", def: WorkflowDefinition{ID: "w", Concurrency: 1, Tasks: []TaskDefinition{{Key: "a", Action: "run", DependsOn: []TaskKey{"missing"}, TimeoutMillis: 1}}}},
		{name: "cycle", def: WorkflowDefinition{ID: "w", Concurrency: 1, Tasks: []TaskDefinition{{Key: "a", Action: "first", DependsOn: []TaskKey{"b"}, TimeoutMillis: 1}, {Key: "b", Action: "second", DependsOn: []TaskKey{"a"}, TimeoutMillis: 1}}}},
		{name: "zero timeout", def: WorkflowDefinition{ID: "w", Concurrency: 1, Tasks: []TaskDefinition{{Key: "a", Action: "run"}}}},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if _, err := Compile(tt.def); err == nil {
				t.Fatal("Compile() error = nil, want non-nil")
			}
		})
	}
}
```

- [ ] **步骤 2：运行测试，确认因类型和 Compile 尚不存在而失败**

运行：`go test ./workflow -run 'TestCompile' -v`

预期：编译失败，包含 `undefined: WorkflowDefinition` 或 `undefined: Compile`。

- [ ] **步骤 3：实现公开定义和 O(V + E) 编译**

```go
// workflow/definition.go
package workflow

// TaskKey 是任务在单个工作流定义中的唯一正式标识。
type TaskKey string

// RetryPolicy 描述任务允许的最大尝试次数和固定重试间隔。
type RetryPolicy struct {
	MaxAttempts   int   `json:"max_attempts,omitempty"`
	IntervalMillis int64 `json:"interval_ms,omitempty"`
}

// TaskDefinition 描述一个任务的动作、依赖、重试和超时配置。
type TaskDefinition struct {
	Key           TaskKey     `json:"key"`
	Action        string      `json:"action"`
	DependsOn     []TaskKey   `json:"depends_on,omitempty"`
	Retry         RetryPolicy `json:"retry,omitempty"`
	TimeoutMillis int64       `json:"timeout_ms"`
}

// WorkflowDefinition 描述可重复创建运行实例的静态工作流配置。
type WorkflowDefinition struct {
	ID          string           `json:"id"`
	Concurrency int              `json:"concurrency"`
	Tasks       []TaskDefinition `json:"tasks"`
}
```

```go
// workflow/compiler.go
package workflow

import (
	"fmt"
	"regexp"
)

var taskKeyPattern = regexp.MustCompile(`^[a-z0-9_-]{1,64}$`)

// CompiledWorkflow 保存定义校验后可由多个运行实例共享的只读索引和 DAG 结构。
type CompiledWorkflow struct {
	definition   *WorkflowDefinition
	index        map[TaskKey]int
	dependencies [][]int
	successors   [][]int
}

// Compile 校验工作流定义，并在线性遍历中建立调度所需的索引和邻接表。
func Compile(def WorkflowDefinition) (*CompiledWorkflow, error) {
	// 深拷贝隔离调用方后续修改，确保编译结果在多个 Run 之间安全共享。
	def = cloneDefinition(def)
	if def.ID == "" {
		return nil, fmt.Errorf("workflow id is required")
	}
	if def.Concurrency <= 0 {
		return nil, fmt.Errorf("concurrency must be greater than zero")
	}
	index := make(map[TaskKey]int, len(def.Tasks))
	for i := range def.Tasks {
		task := &def.Tasks[i]
		if !taskKeyPattern.MatchString(string(task.Key)) {
			return nil, fmt.Errorf("invalid task key %q", task.Key)
		}
		if _, exists := index[task.Key]; exists {
			return nil, fmt.Errorf("duplicate task key %q", task.Key)
		}
		if task.TimeoutMillis <= 0 {
			return nil, fmt.Errorf("task %q timeout must be greater than zero", task.Key)
		}
		if task.Retry.MaxAttempts == 0 {
			task.Retry.MaxAttempts = 1
		}
		if task.Retry.MaxAttempts < 1 || task.Retry.IntervalMillis < 0 {
			return nil, fmt.Errorf("invalid retry policy for task %q", task.Key)
		}
		index[task.Key] = i
	}

	dependencies := make([][]int, len(def.Tasks))
	successors := make([][]int, len(def.Tasks))
	indegree := make([]int, len(def.Tasks))
	for taskIndex, task := range def.Tasks {
		seen := make(map[int]struct{}, len(task.DependsOn))
		for _, dependencyKey := range task.DependsOn {
			dependencyIndex, exists := index[dependencyKey]
			if !exists {
				return nil, fmt.Errorf("task %q depends on missing task %q", task.Key, dependencyKey)
			}
			if _, duplicate := seen[dependencyIndex]; duplicate {
				return nil, fmt.Errorf("task %q repeats dependency %q", task.Key, dependencyKey)
			}
			seen[dependencyIndex] = struct{}{}
			dependencies[taskIndex] = append(dependencies[taskIndex], dependencyIndex)
			successors[dependencyIndex] = append(successors[dependencyIndex], taskIndex)
			indegree[taskIndex]++
		}
	}

	queue := make([]int, 0, len(def.Tasks))
	for i, degree := range indegree {
		if degree == 0 {
			queue = append(queue, i)
		}
	}
	visited := 0
	// Kahn 算法只访问每个任务和每条依赖一次，同时完成环检测并保持 O(V + E)。
	for len(queue) > 0 {
		current := queue[0]
		queue = queue[1:]
		visited++
		for _, next := range successors[current] {
			indegree[next]--
			if indegree[next] == 0 {
				queue = append(queue, next)
			}
		}
	}
	if visited != len(def.Tasks) {
		return nil, fmt.Errorf("workflow contains a dependency cycle")
	}

	return &CompiledWorkflow{definition: &def, index: index, dependencies: dependencies, successors: successors}, nil
}

// cloneDefinition 深拷贝可变切片，避免调用方修改原定义破坏编译结果的不变性。
func cloneDefinition(def WorkflowDefinition) WorkflowDefinition {
	clone := def
	clone.Tasks = make([]TaskDefinition, len(def.Tasks))
	copy(clone.Tasks, def.Tasks)
	for i := range clone.Tasks {
		clone.Tasks[i].DependsOn = append([]TaskKey(nil), def.Tasks[i].DependsOn...)
	}
	return clone
}
```

- [ ] **步骤 4：单独验证 Workflow ID 规则的 RED 和 GREEN**

先在 `workflow/compiler_test.go` 的 import 中加入 `strings`，再增加 ID 边界测试：

```go
func TestCompileRejectsInvalidWorkflowID(t *testing.T) {
	for _, id := range []string{" ", "Bad ID", strings.Repeat("a", 65)} {
		definition := WorkflowDefinition{ID: id, Concurrency: 1, Tasks: []TaskDefinition{{Key: "task", Action: "run", TimeoutMillis: 1000}}}
		_, err := Compile(definition)
		if err == nil || !strings.Contains(err.Error(), "invalid workflow id") {
			t.Fatalf("Compile() id %q error = %v, want invalid workflow id", id, err)
		}
	}
}

func TestCompileAcceptsWorkflowIDAtLengthLimit(t *testing.T) {
	definition := WorkflowDefinition{ID: strings.Repeat("a", 64), Concurrency: 1, Tasks: []TaskDefinition{{Key: "task", Action: "run", TimeoutMillis: 1000}}}
	if _, err := Compile(definition); err != nil {
		t.Fatalf("Compile() error = %v, want nil", err)
	}
}
```

运行：`go test ./workflow -run TestCompileRejectsInvalidWorkflowID -v`

预期：FAIL，至少一个子用例输出 `error = <nil>`，证明编译器尚未执行完整 ID 格式校验。

随后将 `taskKeyPattern` 重命名为供两类机器标识共享的 `identifierPattern`，保留空 ID 的明确错误，并增加格式校验：

```go
var identifierPattern = regexp.MustCompile(`^[a-z0-9_-]{1,64}$`)

if def.ID == "" {
	return nil, fmt.Errorf("workflow id is required")
}
if !identifierPattern.MatchString(def.ID) {
	return nil, fmt.Errorf("invalid workflow id %q", def.ID)
}
```

TaskKey 校验同步改为 `identifierPattern.MatchString(string(task.Key))`，不保留名称含义已经不准确的 `taskKeyPattern`。

运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestCompileRejectsInvalidWorkflowID|TestCompileAcceptsWorkflowIDAtLengthLimit|TestCompileRejectsInvalidDefinitions' -v`

预期：两个测试均 PASS；合法示例 `document-pipeline` 保持可用，空白、大写或空格、超过 64 字符的 ID 被拒绝。

- [ ] **步骤 5：单独验证空工作流校验的 RED 和 GREEN**

在 `workflow/compiler_test.go` 增加只验证空任务列表的测试：

```go
func TestCompileRejectsWorkflowWithoutTasks(t *testing.T) {
	definition := WorkflowDefinition{ID: "empty", Concurrency: 1, Tasks: []TaskDefinition{}}
	_, err := Compile(definition)
	if err == nil || err.Error() != "workflow must contain at least one task" {
		t.Fatalf("Compile() error = %v, want workflow must contain at least one task", err)
	}
}
```

运行：`go test ./workflow -run TestCompileRejectsWorkflowWithoutTasks -v`

预期：FAIL，输出包含 `Compile() error = <nil>`，证明当前编译器错误地接受空任务列表。

随后在 `Compile` 完成 Workflow ID 和 concurrency 校验后加入最小实现：

```go
if len(def.Tasks) == 0 {
	return nil, fmt.Errorf("workflow must contain at least one task")
}
```

运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestCompileRejectsWorkflowWithoutTasks|TestCompileRejectsInvalidDefinitions' -v`

预期：两个测试均 PASS；空任务列表在创建索引或 WorkflowRun 之前被拒绝。

- [ ] **步骤 6：单独验证 Action 非空白校验的 RED 和 GREEN**

复用步骤 4 已导入的 `strings`，增加只验证 Action 的测试：

```go
func TestCompileRejectsBlankAction(t *testing.T) {
	for _, action := range []string{"", " \t"} {
		definition := WorkflowDefinition{ID: "blank-action", Concurrency: 1, Tasks: []TaskDefinition{{Key: "task", Action: action, TimeoutMillis: 1000}}}
		_, err := Compile(definition)
		if err == nil || !strings.Contains(err.Error(), "action is required") {
			t.Fatalf("Compile() action %q error = %v, want action is required", action, err)
		}
	}
}
```

运行：`go test ./workflow -run TestCompileRejectsBlankAction -v`

预期：FAIL，输出包含 `Compile() error = <nil>, want action is required`，证明失败原因是尚未实现 Action 校验，而不是类型、拼写或测试环境错误。

随后在 `workflow/compiler.go` 的 import 中加入 `strings`，并在 TaskKey 格式与重复校验之后、超时校验之前加入最小实现：

```go
if strings.TrimSpace(task.Action) == "" {
	return nil, fmt.Errorf("task %q action is required", task.Key)
}
```

运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestCompileRejectsBlankAction|TestCompileRejectsInvalidDefinitions' -v`

预期：两个测试均 PASS；空字符串和只包含空白字符的 Action 都被拒绝，非空 Action 保持原值，不被编译器擅自裁剪。

- [ ] **步骤 7：验证 10,000 个任务上限和线性链规模边界**

```go
func TestCompileAcceptsTenThousandTasks(t *testing.T) {
	definition := WorkflowDefinition{ID: "large", Concurrency: 16, Tasks: make([]TaskDefinition, 10_000)}
	for i := range definition.Tasks {
		key := TaskKey(fmt.Sprintf("task-%d", i))
		definition.Tasks[i] = TaskDefinition{Key: key, Action: "noop", TimeoutMillis: 1000}
		if i > 0 { definition.Tasks[i].DependsOn = []TaskKey{TaskKey(fmt.Sprintf("task-%d", i-1))} }
	}
	compiled, err := Compile(definition)
	if err != nil { t.Fatal(err) }
	if got := compiled.index["task-9999"]; got != 9999 { t.Fatalf("last index = %d, want 9999", got) }
}

func TestCompileRejectsMoreThanTenThousandTasks(t *testing.T) {
	definition := WorkflowDefinition{ID: "too-large", Concurrency: 16, Tasks: make([]TaskDefinition, 10_001)}
	for i := range definition.Tasks {
		definition.Tasks[i] = TaskDefinition{Key: TaskKey(fmt.Sprintf("task-%d", i)), Action: "noop", TimeoutMillis: 1000}
	}
	_, err := Compile(definition)
	if err == nil || err.Error() != "workflow cannot contain more than 10000 tasks" {
		t.Fatalf("Compile() error = %v, want workflow cannot contain more than 10000 tasks", err)
	}
}
```

先运行超限测试确认 RED：`go test ./workflow -run TestCompileRejectsMoreThanTenThousandTasks -v`

预期：FAIL，输出包含 `Compile() error = <nil>`，证明硬上限尚未实现。

随后在空任务校验之后增加最小实现：

```go
const maxWorkflowTasks = 10_000

if len(def.Tasks) > maxWorkflowTasks {
	return nil, fmt.Errorf("workflow cannot contain more than 10000 tasks")
}
```

再运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestCompileAcceptsTenThousandTasks|TestCompileRejectsMoreThanTenThousandTasks' -v`

预期：两个测试均 PASS。该用例验证 10,000 个任务是允许的硬边界，10,001 个任务在建立索引和分配 DAG 数组前被拒绝；具体时间与内存数据留到任务 10 的 Benchmark 测量。

- [ ] **步骤 8：运行格式化、测试、静态检查和公开注释检查**

运行：`gofmt -w workflow/*.go && go test ./... && go vet ./... && go doc ./workflow`

预期：全部通过，无警告；`go doc` 能显示 TaskKey、RetryPolicy、TaskDefinition、WorkflowDefinition、CompiledWorkflow 和 Compile 的中文说明。人工核对编译结果共享与 Kahn 环检测注释解释了原因和不变量，没有逐行复述代码。

- [ ] **步骤 9：暂停供用户审核并提示手动提交**

建议 commit：`实现工作流定义校验与DAG编译`

---

### 任务 2：实现状态机、运行快照和依赖计数

**文件：**

- 新建：`workflow/state.go`
- 新建：`workflow/run.go`
- 新建：`workflow/state_test.go`

**接口：**

- 输入：`CompiledWorkflow`、RunID 和当前时间。
- 输出：`newRunSnapshot`、三类状态常量、合法状态转换和每个 Run 独立的 `RemainingDependencies`。
- 后续依赖：Store 持久化 `RunSnapshot`；Engine 只通过状态转换函数修改状态。

- [ ] **步骤 1：先写状态转换和运行初始化失败测试**

```go
package workflow

import (
	"testing"
	"time"
)

func TestTaskTransitionRejectsIllegalJump(t *testing.T) {
	snapshot := RunSnapshot{Run: WorkflowRun{Tasks: []TaskRun{{Key: "clean", Status: TaskWaitingDependencies}}}}
	if err := transitionTask(&snapshot, 0, TaskSucceeded, time.Unix(1, 0), "illegal jump"); err == nil {
		t.Fatal("transitionTask() error = nil, want illegal transition")
	}
}

func TestNewRunSnapshotHasIndependentDependencyCounts(t *testing.T) {
	compiled, err := Compile(WorkflowDefinition{ID: "w", Concurrency: 1, Tasks: []TaskDefinition{
		{Key: "a", Action: "a", TimeoutMillis: 1},
		{Key: "b", Action: "b", DependsOn: []TaskKey{"a"}, TimeoutMillis: 1},
	}})
	if err != nil {
		t.Fatal(err)
	}
	one := newRunSnapshot("run-one", compiled, time.Unix(1, 0))
	two := newRunSnapshot("run-two", compiled, time.Unix(1, 0))
	one.Run.RemainingDependencies[1]--
	if got := two.Run.RemainingDependencies[1]; got != 1 {
		t.Fatalf("second run remaining dependency = %d, want 1", got)
	}
}
```

- [ ] **步骤 2：确认测试因状态类型和初始化函数缺失而失败**

运行：`go test ./workflow -run 'TestTaskTransition|TestNewRunSnapshot' -v`

预期：编译失败，包含 `undefined: TaskRun` 或 `undefined: newRunSnapshot`。

- [ ] **步骤 3：实现状态、Attempt、事件和快照结构**

```go
// workflow/state.go
package workflow

import (
	"fmt"
	"time"
)

// WorkflowStatus 表示一次工作流运行的生命周期状态。
type WorkflowStatus string
// TaskStatus 表示某个任务在一次运行中的调度状态。
type TaskStatus string
// AttemptStatus 表示任务单次执行尝试的结果状态。
type AttemptStatus string

const (
	WorkflowPending   WorkflowStatus = "pending"
	WorkflowRunning   WorkflowStatus = "running"
	WorkflowSucceeded WorkflowStatus = "succeeded"
	WorkflowFailed    WorkflowStatus = "failed"
	WorkflowCanceled  WorkflowStatus = "canceled"
)

const (
	TaskWaitingDependencies TaskStatus = "waiting_dependencies"
	TaskReady               TaskStatus = "ready"
	TaskRunning             TaskStatus = "running"
	TaskWaitingRetry        TaskStatus = "waiting_retry"
	TaskSucceeded           TaskStatus = "succeeded"
	TaskFailed              TaskStatus = "failed"
	TaskCanceled            TaskStatus = "canceled"
	TaskSkipped             TaskStatus = "skipped"
)

const (
	AttemptRunning     AttemptStatus = "running"
	AttemptSucceeded   AttemptStatus = "succeeded"
	AttemptFailed      AttemptStatus = "failed"
	AttemptTimedOut    AttemptStatus = "timed_out"
	AttemptCanceled    AttemptStatus = "canceled"
	AttemptInterrupted AttemptStatus = "interrupted"
)

var taskTransitions = map[TaskStatus]map[TaskStatus]bool{
	TaskWaitingDependencies: {TaskReady: true, TaskCanceled: true, TaskSkipped: true},
	TaskReady:               {TaskRunning: true, TaskCanceled: true, TaskSkipped: true},
	TaskRunning:             {TaskSucceeded: true, TaskWaitingRetry: true, TaskFailed: true, TaskCanceled: true},
	TaskWaitingRetry:        {TaskReady: true, TaskFailed: true, TaskCanceled: true},
}

func transitionTask(snapshot *RunSnapshot, taskIndex int, to TaskStatus, at time.Time, reason string) error {
	task := &snapshot.Run.Tasks[taskIndex]
	from := task.Status
	if !taskTransitions[task.Status][to] {
		return fmt.Errorf("illegal task transition %s -> %s", task.Status, to)
	}
	task.Status = to
	if to == TaskSucceeded || to == TaskFailed || to == TaskCanceled || to == TaskSkipped {
		task.FinishedAt = &at
	}
	appendEvent(snapshot, at, "task", string(task.Key), string(from), string(to), reason)
	return nil
}
```

```go
// workflow/run.go
package workflow

import "time"

// RunID 是工作流运行实例的唯一标识。
type RunID string

// ExecutionResult 保存执行器返回的最小结果和错误信息。
type ExecutionResult struct {
	Output       string `json:"output,omitempty"`
	ErrorCode    string `json:"error_code,omitempty"`
	ErrorMessage string `json:"error_message,omitempty"`
}

// Attempt 记录任务的一次具体执行，不会被后续重试覆盖。
type Attempt struct {
	Number     int             `json:"number"`
	Status     AttemptStatus   `json:"status"`
	StartedAt  time.Time       `json:"started_at"`
	FinishedAt *time.Time      `json:"finished_at,omitempty"`
	Result     ExecutionResult `json:"result"`
}

// TaskRun 保存任务在某次工作流运行中的状态和全部尝试历史。
type TaskRun struct {
	Key        TaskKey        `json:"key"`
	Status     TaskStatus     `json:"status"`
	Attempts   []Attempt      `json:"attempts"`
	ReadyAt    *time.Time     `json:"ready_at,omitempty"`
	FinishedAt *time.Time     `json:"finished_at,omitempty"`
}

// StateEvent 记录一次已持久化状态转换的审计信息。
type StateEvent struct {
	Sequence uint64    `json:"sequence"`
	At       time.Time `json:"at"`
	Entity   string    `json:"entity"`
	Key      string    `json:"key"`
	From     string    `json:"from"`
	To       string    `json:"to"`
	Reason   string    `json:"reason,omitempty"`
}

// WorkflowRun 保存一次工作流运行的状态、任务和依赖计数。
type WorkflowRun struct {
	ID                    RunID          `json:"id"`
	DefinitionID          string         `json:"definition_id"`
	Status                WorkflowStatus `json:"status"`
	Tasks                 []TaskRun      `json:"tasks"`
	RemainingDependencies []int          `json:"remaining_dependencies"`
	CreatedAt             time.Time      `json:"created_at"`
	StartedAt             *time.Time     `json:"started_at,omitempty"`
	FinishedAt            *time.Time     `json:"finished_at,omitempty"`
}

// RunSnapshot 是 Store 原子读写的完整持久化单元。
type RunSnapshot struct {
	Version    int                 `json:"version"`
	Definition *WorkflowDefinition `json:"definition"`
	Run        WorkflowRun         `json:"run"`
	Events     []StateEvent        `json:"events"`
}

func newRunSnapshot(id RunID, compiled *CompiledWorkflow, now time.Time) RunSnapshot {
	tasks := make([]TaskRun, len(compiled.definition.Tasks))
	remaining := make([]int, len(tasks))
	for i, task := range compiled.definition.Tasks {
		status := TaskWaitingDependencies
		remaining[i] = len(compiled.dependencies[i])
		if remaining[i] == 0 {
			status = TaskReady
		}
		tasks[i] = TaskRun{Key: task.Key, Status: status, Attempts: []Attempt{}}
	}
	return RunSnapshot{Version: 1, Definition: compiled.definition, Run: WorkflowRun{ID: id, DefinitionID: compiled.definition.ID, Status: WorkflowPending, Tasks: tasks, RemainingDependencies: remaining, CreatedAt: now}, Events: []StateEvent{}}
}
```

- [ ] **步骤 4：先补充 Workflow、Attempt 和事件失败测试**

```go
func TestWorkflowTransitionAppendsSequencedEvent(t *testing.T) {
	snapshot := RunSnapshot{Run: WorkflowRun{ID: "run-one", Status: WorkflowPending}}
	at := time.Unix(2, 0)
	if err := transitionWorkflow(&snapshot, WorkflowRunning, at, "execute"); err != nil { t.Fatal(err) }
	if snapshot.Run.StartedAt == nil || !snapshot.Run.StartedAt.Equal(at) { t.Fatalf("started_at = %v", snapshot.Run.StartedAt) }
	if len(snapshot.Events) != 1 || snapshot.Events[0].Sequence != 1 || snapshot.Events[0].Entity != "workflow" {
		t.Fatalf("events = %+v", snapshot.Events)
	}
	if err := transitionWorkflow(&snapshot, WorkflowPending, at, "illegal"); err == nil {
		t.Fatal("transitionWorkflow() error = nil, want illegal transition")
	}
}

func TestAttemptTransitionRejectsTerminalStateExit(t *testing.T) {
	attempt := Attempt{Number: 1, Status: AttemptSucceeded}
	snapshot := RunSnapshot{Run: WorkflowRun{Tasks: []TaskRun{{Key: "clean", Attempts: []Attempt{attempt}}}}}
	if err := transitionAttempt(&snapshot, "clean", &snapshot.Run.Tasks[0].Attempts[0], AttemptRunning, time.Unix(3, 0), "illegal exit"); err == nil {
		t.Fatal("transitionAttempt() error = nil, want illegal transition")
	}
}
```

运行：`go test ./workflow -run 'TestWorkflowTransition|TestAttemptTransition' -v`

预期：编译失败，包含 `undefined: transitionWorkflow` 或 `undefined: transitionAttempt`。

- [ ] **步骤 5：实现完整转换表和统一事件追加**

```go
var workflowTransitions = map[WorkflowStatus]map[WorkflowStatus]bool{
	WorkflowPending: {WorkflowRunning: true, WorkflowCanceled: true},
	WorkflowRunning: {WorkflowSucceeded: true, WorkflowFailed: true, WorkflowCanceled: true},
}

var attemptTransitions = map[AttemptStatus]map[AttemptStatus]bool{
	AttemptRunning: {
		AttemptSucceeded: true, AttemptFailed: true, AttemptTimedOut: true,
		AttemptCanceled: true, AttemptInterrupted: true,
	},
}

func transitionWorkflow(snapshot *RunSnapshot, to WorkflowStatus, at time.Time, reason string) error {
	from := snapshot.Run.Status
	if !workflowTransitions[from][to] { return fmt.Errorf("illegal workflow transition %s -> %s", from, to) }
	snapshot.Run.Status = to
	if to == WorkflowRunning && snapshot.Run.StartedAt == nil { snapshot.Run.StartedAt = &at }
	if isWorkflowTerminal(to) { snapshot.Run.FinishedAt = &at }
	appendEvent(snapshot, at, "workflow", string(snapshot.Run.ID), string(from), string(to), reason)
	return nil
}

func transitionAttempt(snapshot *RunSnapshot, taskKey TaskKey, attempt *Attempt, to AttemptStatus, at time.Time, reason string) error {
	from := attempt.Status
	if !attemptTransitions[from][to] { return fmt.Errorf("illegal attempt transition %s -> %s", from, to) }
	attempt.Status, attempt.FinishedAt = to, &at
	appendEvent(snapshot, at, "attempt", fmt.Sprintf("%s/%d", taskKey, attempt.Number), string(from), string(to), reason)
	return nil
}

func appendEvent(snapshot *RunSnapshot, at time.Time, entity, key, from, to, reason string) {
	snapshot.Events = append(snapshot.Events, StateEvent{Sequence: uint64(len(snapshot.Events) + 1), At: at, Entity: entity, Key: key, From: from, To: to, Reason: reason})
}

func isWorkflowTerminal(status WorkflowStatus) bool {
	return status == WorkflowSucceeded || status == WorkflowFailed || status == WorkflowCanceled
}
```

`transitionTask` 同样改为接收 `*RunSnapshot` 和 task 下标，在成功转换后调用 `appendEvent`；Engine 不得直接赋值改变 Workflow、Task 或 Attempt 状态。再增加两个 Run 的 Tasks、Attempts、RemainingDependencies 和 Events 互不共享底层切片的断言。

- [ ] **步骤 6：运行状态测试和全量回归**

运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestTaskTransition|TestNewRunSnapshot|TestWorkflowTransition|TestAttemptTransition' -v && go test ./... && go vet ./...`

预期：全部 PASS。

- [ ] **步骤 7：暂停供用户审核并提示手动提交**

建议 commit：`实现工作流运行状态机与依赖计数`

---

### 任务 3：实现 Store 接口和原子 JSON 文件存储

**文件：**

- 新建：`workflow/store.go`
- 新建：`workflow/filestore/store.go`
- 新建：`workflow/filestore/store_test.go`

**接口：**

- 输入：完整 `RunSnapshot`。
- 输出：`Store.Create`、`Store.Save`、`Store.Load`。
- 错误：`ErrRunExists`、`ErrRunNotFound`、`ErrAtomicReplaceUnsupported`。

- [ ] **步骤 1：先写文件存储失败测试**

```go
package filestore

import (
	"context"
	"errors"
	"testing"
	"time"

	"github.com/fhtyfgty5-eng/ai-workload-platform/workflow"
)

func TestStoreCreateSaveLoad(t *testing.T) {
	store, err := New(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	snapshot := workflow.RunSnapshot{Version: 1, Run: workflow.WorkflowRun{ID: "run-one", DefinitionID: "w", Status: workflow.WorkflowPending, CreatedAt: time.Unix(1, 0)}}
	if err := store.Create(context.Background(), snapshot); err != nil {
		t.Fatal(err)
	}
	snapshot.Run.Status = workflow.WorkflowRunning
	if err := store.Save(context.Background(), snapshot); err != nil {
		t.Fatal(err)
	}
	loaded, err := store.Load(context.Background(), "run-one")
	if err != nil {
		t.Fatal(err)
	}
	if loaded.Run.Status != workflow.WorkflowRunning {
		t.Fatalf("status = %s, want running", loaded.Run.Status)
	}
}

func TestStoreDoesNotReplaceValidSnapshotWhenRenameFails(t *testing.T) {
	dir := t.TempDir()
	store, err := New(dir)
	if err != nil {
		t.Fatal(err)
	}
	original := workflow.RunSnapshot{Version: 1, Run: workflow.WorkflowRun{ID: "run-one", Status: workflow.WorkflowPending, CreatedAt: time.Unix(1, 0)}}
	if err := store.Create(context.Background(), original); err != nil {
		t.Fatal(err)
	}
	store.rename = func(string, string) error { return errors.New("rename failed") }
	original.Run.Status = workflow.WorkflowRunning
	if err := store.Save(context.Background(), original); err == nil {
		t.Fatal("Save() error = nil, want rename failure")
	}
	loaded, err := store.Load(context.Background(), "run-one")
	if err != nil {
		t.Fatal(err)
	}
	if loaded.Run.Status != workflow.WorkflowPending {
		t.Fatalf("status = %s, want pending", loaded.Run.Status)
	}
}
```

- [ ] **步骤 2：确认测试因 Store 和 New 缺失而失败**

运行：`go test ./workflow/filestore -v`

预期：编译失败，包含 `undefined: New` 或 Store 方法缺失。

- [ ] **步骤 3：定义 Store 并实现临时文件原子替换**

```go
// workflow/store.go
package workflow

import (
	"context"
	"errors"
)

var (
	ErrRunExists               = errors.New("workflow run already exists")
	ErrRunNotFound             = errors.New("workflow run not found")
	ErrAtomicReplaceUnsupported = errors.New("atomic file replacement is unsupported on this platform")
)

// Store 持久化完整运行快照；Engine 只在保存成功后推进调度。
type Store interface {
	// Create 持久化新 Run，存在相同 RunID 时返回 ErrRunExists。
	Create(context.Context, RunSnapshot) error
	// Save 原子更新已有 Run，不存在时返回 ErrRunNotFound。
	Save(context.Context, RunSnapshot) error
	// Load 返回指定 RunID 的完整快照。
	Load(context.Context, RunID) (RunSnapshot, error)
}
```

```go
// workflow/filestore/store.go
package filestore

import (
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"os"
	"path/filepath"
	"runtime"
	"strings"
	"sync"

	"github.com/fhtyfgty5-eng/ai-workload-platform/workflow"
)

// Store 使用每个 Run 一个 JSON 文件保存工作流快照。
type Store struct {
	dir    string
	mu     sync.Mutex
	rename func(string, string) error
}

// New 创建仅支持 macOS 和 Linux 原子替换语义的文件 Store。
func New(dir string) (*Store, error) {
	if runtime.GOOS != "darwin" && runtime.GOOS != "linux" {
		return nil, workflow.ErrAtomicReplaceUnsupported
	}
	if err := os.MkdirAll(dir, 0o755); err != nil {
		return nil, fmt.Errorf("create store directory: %w", err)
	}
	return &Store{dir: dir, rename: os.Rename}, nil
}

func (s *Store) path(id workflow.RunID) (string, error) {
	value := string(id)
	if value == "" || strings.ContainsAny(value, `/\\`) || value == "." || value == ".." {
		return "", fmt.Errorf("invalid run id %q", value)
	}
	return filepath.Join(s.dir, value+".json"), nil
}

// Create 创建新的运行快照，已存在的 RunID 不会被覆盖。
func (s *Store) Create(ctx context.Context, snapshot workflow.RunSnapshot) error {
	path, err := s.path(snapshot.Run.ID)
	if err != nil {
		return err
	}
	s.mu.Lock()
	defer s.mu.Unlock()
	if _, err := os.Stat(path); err == nil {
		return workflow.ErrRunExists
	} else if !errors.Is(err, os.ErrNotExist) {
		return err
	}
	return s.writeLocked(ctx, path, snapshot)
}

// Save 使用同目录临时文件和原子替换更新已有快照。
func (s *Store) Save(ctx context.Context, snapshot workflow.RunSnapshot) error {
	path, err := s.path(snapshot.Run.ID)
	if err != nil {
		return err
	}
	s.mu.Lock()
	defer s.mu.Unlock()
	if _, err := os.Stat(path); err != nil {
		if errors.Is(err, os.ErrNotExist) {
			return workflow.ErrRunNotFound
		}
		return err
	}
	return s.writeLocked(ctx, path, snapshot)
}

func (s *Store) writeLocked(ctx context.Context, target string, snapshot workflow.RunSnapshot) error {
	if err := ctx.Err(); err != nil {
		return err
	}
	data, err := json.MarshalIndent(snapshot, "", "  ")
	if err != nil {
		return fmt.Errorf("encode snapshot: %w", err)
	}
	// 临时文件必须位于目标目录，避免跨文件系统 Rename 破坏原子替换语义。
	temp, err := os.CreateTemp(s.dir, ".snapshot-*.tmp")
	if err != nil {
		return fmt.Errorf("create temporary snapshot: %w", err)
	}
	tempName := temp.Name()
	defer os.Remove(tempName)
	if _, err := temp.Write(data); err != nil {
		temp.Close()
		return fmt.Errorf("write temporary snapshot: %w", err)
	}
	if err := temp.Sync(); err != nil {
		temp.Close()
		return fmt.Errorf("sync temporary snapshot: %w", err)
	}
	if err := temp.Close(); err != nil {
		return fmt.Errorf("close temporary snapshot: %w", err)
	}
	if err := s.rename(tempName, target); err != nil {
		return fmt.Errorf("replace snapshot: %w", err)
	}
	// 文件同步后还要同步目录，确保崩溃恢复时目录项更新已经持久化。
	directory, err := os.Open(s.dir)
	if err != nil { return fmt.Errorf("open snapshot directory: %w", err) }
	defer directory.Close()
	if err := directory.Sync(); err != nil { return fmt.Errorf("sync snapshot directory: %w", err) }
	return nil
}

// Load 读取并解码指定 RunID 的完整快照。
func (s *Store) Load(ctx context.Context, id workflow.RunID) (workflow.RunSnapshot, error) {
	if err := ctx.Err(); err != nil {
		return workflow.RunSnapshot{}, err
	}
	path, err := s.path(id)
	if err != nil {
		return workflow.RunSnapshot{}, err
	}
	data, err := os.ReadFile(path)
	if errors.Is(err, os.ErrNotExist) {
		return workflow.RunSnapshot{}, workflow.ErrRunNotFound
	}
	if err != nil {
		return workflow.RunSnapshot{}, err
	}
	var snapshot workflow.RunSnapshot
	if err := json.Unmarshal(data, &snapshot); err != nil {
		return workflow.RunSnapshot{}, fmt.Errorf("decode snapshot: %w", err)
	}
	return snapshot, nil
}
```

- [ ] **步骤 4：补充文件损坏、重复创建、路径穿越和平台边界测试**

测试必须断言：损坏 JSON 返回解码错误；重复 Create 返回 `ErrRunExists`；两个 goroutine 通过同一个 Store 实例并发 Create 相同 RunID 时恰好一个成功、另一个返回 `ErrRunExists`；缺失文件返回 `ErrRunNotFound`；带 `/`、`\\`、`.`、`..` 的 RunID 被拒绝；macOS/Linux 允许创建，其他平台返回 `ErrAtomicReplaceUnsupported`。模块 1 只承诺单进程内的写互斥；不同进程并发写同一 Run 文件不在本模块支持范围内。

- [ ] **步骤 5：运行存储测试和全量回归**

运行：`gofmt -w workflow/*.go workflow/filestore/*.go && go test ./workflow/filestore -v && go test ./... && go vet ./...`

预期：全部 PASS。

- [ ] **步骤 6：暂停供用户审核并提示手动提交**

建议 commit：`实现工作流JSON快照存储`

---

### 任务 4：实现 Clock、Executor 和 Mock Executor

**文件：**

- 新建：`workflow/clock.go`
- 新建：`workflow/executor.go`
- 新建：`workflow/mockexec/executor.go`
- 新建：`workflow/mockexec/executor_test.go`

**接口：**

- `Clock.Now() time.Time` 和 `Clock.After(time.Duration) <-chan time.Time`。
- `Executor.Execute(context.Context, ExecutionRequest) ExecutionResponse`。
- `mockexec.New(Clock, map[string][]Step) *Executor`，每个 Action 按顺序消费 Step。

- [ ] **步骤 1：先写 Mock Executor 失败测试**

```go
package mockexec

import (
	"context"
	"testing"

	"github.com/fhtyfgty5-eng/ai-workload-platform/workflow"
)

func TestExecutorConsumesScriptInOrder(t *testing.T) {
	executor := New(workflow.RealClock{}, map[string][]Step{
		"mock-clean": {
			{Kind: workflow.ResultTemporaryFailure, ErrorCode: "temporary"},
			{Kind: workflow.ResultSuccess, Output: "cleaned"},
		},
	})
	request := workflow.ExecutionRequest{RunID: "run-one", TaskKey: "clean", Action: "mock-clean", Attempt: 1}
	first := executor.Execute(context.Background(), request)
	request.Attempt = 2
	second := executor.Execute(context.Background(), request)
	if first.Kind != workflow.ResultTemporaryFailure || second.Kind != workflow.ResultSuccess {
		t.Fatalf("results = %s, %s", first.Kind, second.Kind)
	}
}

func TestExecutorReturnsCanceledWhenContextIsCanceled(t *testing.T) {
	ctx, cancel := context.WithCancel(context.Background())
	cancel()
	executor := New(workflow.RealClock{}, map[string][]Step{"mock-clean": {{WaitForCancellation: true}}})
	result := executor.Execute(ctx, workflow.ExecutionRequest{Action: "mock-clean"})
	if result.Kind != workflow.ResultCanceled {
		t.Fatalf("kind = %s, want canceled", result.Kind)
	}
}
```

- [ ] **步骤 2：确认测试因接口和实现缺失而失败**

运行：`go test ./workflow/mockexec -v`

预期：编译失败，包含 `undefined: New`、`undefined: ResultTemporaryFailure` 或接口缺失。

- [ ] **步骤 3：实现 Clock 与 Executor 协议**

```go
// workflow/clock.go
package workflow

import "time"

// Clock 隔离当前时间和等待能力，使重试与超时测试不依赖真实时间流逝。
type Clock interface {
	// Now 返回当前时间。
	Now() time.Time
	// After 返回在指定时长后可接收值的通道。
	After(time.Duration) <-chan time.Time
}

// RealClock 使用操作系统的真实时间实现 Clock。
type RealClock struct{}

// Now 返回操作系统当前时间。
func (RealClock) Now() time.Time { return time.Now() }
// After 使用真实定时器等待指定时长。
func (RealClock) After(d time.Duration) <-chan time.Time { return time.After(d) }
```

```go
// workflow/executor.go
package workflow

import "context"

// ResultKind 是 Executor 返回给 Engine 的结构化结果分类。
type ResultKind string

const (
	ResultSuccess          ResultKind = "success"
	ResultTemporaryFailure ResultKind = "temporary_failure"
	ResultPermanentFailure ResultKind = "permanent_failure"
	ResultCanceled         ResultKind = "canceled"
)

// ExecutionRequest 描述一次具体任务尝试的执行输入。
type ExecutionRequest struct {
	RunID   RunID
	TaskKey TaskKey
	Action  string
	Attempt int
}

// ExecutionResponse 保存 Executor 的结构化结果，不直接改变调度状态。
type ExecutionResponse struct {
	Kind         ResultKind
	Output       string
	ErrorCode    string
	ErrorMessage string
}

// Executor 执行单次任务尝试，并通过 Context 响应超时和取消。
type Executor interface {
	// Execute 执行一次任务尝试并返回结构化结果。
	Execute(context.Context, ExecutionRequest) ExecutionResponse
}
```

- [ ] **步骤 4：实现可编程 Mock Executor**

```go
package mockexec

import (
	"context"
	"sync"
	"time"

	"github.com/fhtyfgty5-eng/ai-workload-platform/workflow"
)

// Step 描述 Mock Executor 为某个 Action 预设的一次行为。
type Step struct {
	Kind                workflow.ResultKind
	Output              string
	ErrorCode           string
	ErrorMessage        string
	Delay               time.Duration
	WaitForCancellation bool
}

// Executor 按 Action 顺序消费预设 Step，用于可重复的内核测试。
type Executor struct {
	clock   workflow.Clock
	mu      sync.Mutex
	scripts map[string][]Step
	next    map[string]int
}

// New 创建使用给定可控时钟和动作脚本的 Mock Executor。
func New(clock workflow.Clock, scripts map[string][]Step) *Executor {
	return &Executor{clock: clock, scripts: scripts, next: make(map[string]int)}
}

// Execute 消费对应 Action 的下一个 Step，并协作响应取消信号。
func (e *Executor) Execute(ctx context.Context, request workflow.ExecutionRequest) workflow.ExecutionResponse {
	e.mu.Lock()
	steps := e.scripts[request.Action]
	index := e.next[request.Action]
	if index >= len(steps) {
		e.mu.Unlock()
		return workflow.ExecutionResponse{Kind: workflow.ResultPermanentFailure, ErrorCode: "script_exhausted", ErrorMessage: "no scripted result"}
	}
	step := steps[index]
	e.next[request.Action] = index + 1
	e.mu.Unlock()

	if step.WaitForCancellation {
		<-ctx.Done()
		return workflow.ExecutionResponse{Kind: workflow.ResultCanceled, ErrorCode: "canceled", ErrorMessage: ctx.Err().Error()}
	}
	if step.Delay > 0 {
		select {
		case <-ctx.Done():
			return workflow.ExecutionResponse{Kind: workflow.ResultCanceled, ErrorCode: "canceled", ErrorMessage: ctx.Err().Error()}
		case <-e.clock.After(step.Delay):
		}
	}
	return workflow.ExecutionResponse{Kind: step.Kind, Output: step.Output, ErrorCode: step.ErrorCode, ErrorMessage: step.ErrorMessage}
}
```

- [ ] **步骤 5：运行 Mock Executor 测试和全量回归**

运行：`gofmt -w workflow/*.go workflow/mockexec/*.go && go test ./workflow/mockexec -v && go test ./... && go vet ./...`

预期：全部 PASS。

- [ ] **步骤 6：暂停供用户审核并提示手动提交**

建议 commit：`实现任务执行协议与模拟执行器`

---

### 任务 5：实现成功路径调度和并发上限

**文件：**

- 新建：`workflow/engine.go`
- 新建：`workflow/engine_test.go`
- 新建：`workflow/test_helpers_test.go`

**接口：**

- `NewEngine(Store, Executor, EngineOptions) (*Engine, error)`。
- `CreateRun(context.Context, *CompiledWorkflow) (RunID, error)`。
- `Execute(context.Context, RunID) (WorkflowRun, error)`。
- `GetRun(context.Context, RunID) (WorkflowRun, error)`。

**共享测试辅助对象：** `workflow/test_helpers_test.go` 必须定义下列对象，后续任务只能复用这些确定接口，不得临时发明同名但语义不同的测试桩。

```go
package workflow

import (
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"runtime"
	"sync"
	"testing"
	"time"
)

func cloneSnapshot(in RunSnapshot) RunSnapshot {
	data, _ := json.Marshal(in)
	var out RunSnapshot
	_ = json.Unmarshal(data, &out)
	return out
}

type memoryStore struct {
	mu        sync.Mutex
	snapshots map[RunID]RunSnapshot
	writes    int
	failAt    int
}

func newMemoryStore() *memoryStore { return &memoryStore{snapshots: make(map[RunID]RunSnapshot)} }
func newFailingStore(failAt int) *memoryStore { s := newMemoryStore(); s.failAt = failAt; return s }
func (s *memoryStore) mutate() error {
	s.writes++
	if s.failAt > 0 && s.writes == s.failAt { return errors.New("injected store failure") }
	return nil
}
func (s *memoryStore) Create(ctx context.Context, snapshot RunSnapshot) error {
	if err := ctx.Err(); err != nil { return err }
	s.mu.Lock(); defer s.mu.Unlock()
	if err := s.mutate(); err != nil { return err }
	if _, exists := s.snapshots[snapshot.Run.ID]; exists { return ErrRunExists }
	s.snapshots[snapshot.Run.ID] = cloneSnapshot(snapshot)
	return nil
}
func (s *memoryStore) Save(ctx context.Context, snapshot RunSnapshot) error {
	if err := ctx.Err(); err != nil { return err }
	s.mu.Lock(); defer s.mu.Unlock()
	if err := s.mutate(); err != nil { return err }
	if _, exists := s.snapshots[snapshot.Run.ID]; !exists { return ErrRunNotFound }
	s.snapshots[snapshot.Run.ID] = cloneSnapshot(snapshot)
	return nil
}
func (s *memoryStore) Load(ctx context.Context, id RunID) (RunSnapshot, error) {
	if err := ctx.Err(); err != nil { return RunSnapshot{}, err }
	s.mu.Lock(); defer s.mu.Unlock()
	snapshot, exists := s.snapshots[id]
	if !exists { return RunSnapshot{}, ErrRunNotFound }
	return cloneSnapshot(snapshot), nil
}

type manualTimer struct { at time.Time; ch chan time.Time }
type manualClock struct { mu sync.Mutex; now time.Time; timers []manualTimer }
func newManualClock(now time.Time) *manualClock { return &manualClock{now: now} }
func (c *manualClock) Now() time.Time { c.mu.Lock(); defer c.mu.Unlock(); return c.now }
func (c *manualClock) After(d time.Duration) <-chan time.Time {
	c.mu.Lock(); defer c.mu.Unlock()
	ch := make(chan time.Time, 1)
	if d <= 0 { ch <- c.now; close(ch); return ch }
	c.timers = append(c.timers, manualTimer{at: c.now.Add(d), ch: ch})
	return ch
}
func (c *manualClock) Advance(d time.Duration) {
	c.mu.Lock()
	c.now = c.now.Add(d)
	remaining := c.timers[:0]
	for _, timer := range c.timers {
		if !timer.at.After(c.now) { timer.ch <- c.now; close(timer.ch) } else { remaining = append(remaining, timer) }
	}
	c.timers = remaining
	c.mu.Unlock()
}
func (c *manualClock) waitForTimers(t *testing.T, count int) { t.Helper(); deadline := time.Now().Add(time.Second); for { c.mu.Lock(); got := len(c.timers); c.mu.Unlock(); if got >= count { return }; if time.Now().After(deadline) { t.Fatalf("timer count = %d, want at least %d", got, count) }; runtime.Gosched() } }

type recordedCall struct { action string; sequence int }
type recordingExecutor struct { mu sync.Mutex; responses map[string]ExecutionResponse; calls []recordedCall }
func newRecordingExecutor(responses map[string]ExecutionResponse) *recordingExecutor { return &recordingExecutor{responses: responses} }
func (e *recordingExecutor) Execute(_ context.Context, request ExecutionRequest) ExecutionResponse {
	e.mu.Lock(); defer e.mu.Unlock()
	e.calls = append(e.calls, recordedCall{action: request.Action, sequence: len(e.calls)})
	if response, ok := e.responses[request.Action]; ok { return response }
	return ExecutionResponse{Kind: ResultSuccess}
}
func (e *recordingExecutor) callCount() int { e.mu.Lock(); defer e.mu.Unlock(); return len(e.calls) }
func (e *recordingExecutor) wasCalled(action string) bool { e.mu.Lock(); defer e.mu.Unlock(); for _, call := range e.calls { if call.action == action { return true } }; return false }
func (e *recordingExecutor) startedBefore(action, dependency string) bool {
	e.mu.Lock(); defer e.mu.Unlock()
	positions := map[string]int{}; for _, call := range e.calls { positions[call.action] = call.sequence }
	a, aOK := positions[action]; d, dOK := positions[dependency]
	return aOK && (!dOK || a < d)
}

type gateExecutor struct { mu sync.Mutex; active, maximum int; started chan struct{}; release chan struct{}; once sync.Once }
func newGateExecutor() *gateExecutor { return &gateExecutor{started: make(chan struct{}, 1024), release: make(chan struct{})} }
func (e *gateExecutor) Execute(ctx context.Context, _ ExecutionRequest) ExecutionResponse {
	e.mu.Lock(); e.active++; if e.active > e.maximum { e.maximum = e.active }; e.mu.Unlock()
	e.started <- struct{}{}
	select { case <-ctx.Done(): case <-e.release: }
	e.mu.Lock(); e.active--; e.mu.Unlock()
	if ctx.Err() != nil { return ExecutionResponse{Kind: ResultCanceled} }
	return ExecutionResponse{Kind: ResultSuccess}
}
func (e *gateExecutor) waitForStarted(t *testing.T, count int) { t.Helper(); for i := 0; i < count; i++ { select { case <-e.started: case <-time.After(time.Second): t.Fatal("executor did not start") } } }
func (e *gateExecutor) maxActive() int { e.mu.Lock(); defer e.mu.Unlock(); return e.maximum }
func (e *gateExecutor) releaseAll() { e.once.Do(func() { close(e.release) }) }

type sequenceExecutor struct { mu sync.Mutex; responses []ExecutionResponse; calls chan struct{} }
func newSequenceExecutor(responses []ExecutionResponse) *sequenceExecutor { return &sequenceExecutor{responses: responses, calls: make(chan struct{}, 1024)} }
func (e *sequenceExecutor) Execute(_ context.Context, _ ExecutionRequest) ExecutionResponse { e.mu.Lock(); response := e.responses[0]; e.responses = e.responses[1:]; e.mu.Unlock(); e.calls <- struct{}{}; return response }
func (e *sequenceExecutor) waitForCalls(t *testing.T, count int) { t.Helper(); for i := 0; i < count; i++ { select { case <-e.calls: case <-time.After(time.Second): t.Fatal("executor call not observed") } } }

type blockingThenSuccessExecutor struct { mu sync.Mutex; calls int; started chan struct{} }
func newBlockingThenSuccessExecutor() *blockingThenSuccessExecutor { return &blockingThenSuccessExecutor{started: make(chan struct{}, 2)} }
func (e *blockingThenSuccessExecutor) Execute(ctx context.Context, _ ExecutionRequest) ExecutionResponse { e.mu.Lock(); e.calls++; call := e.calls; e.mu.Unlock(); e.started <- struct{}{}; if call == 1 { <-ctx.Done(); return ExecutionResponse{Kind: ResultCanceled} }; return ExecutionResponse{Kind: ResultSuccess} }
func (e *blockingThenSuccessExecutor) waitForCalls(t *testing.T, count int) { t.Helper(); for i := 0; i < count; i++ { select { case <-e.started: case <-time.After(time.Second): t.Fatal("executor call not observed") } } }

type cancelAwareExecutor struct { started chan struct{} }
func newCancelAwareExecutor() *cancelAwareExecutor { return &cancelAwareExecutor{started: make(chan struct{})} }
func (e *cancelAwareExecutor) Execute(ctx context.Context, _ ExecutionRequest) ExecutionResponse { close(e.started); <-ctx.Done(); return ExecutionResponse{Kind: ResultCanceled} }
func (e *cancelAwareExecutor) waitUntilStarted(t *testing.T) { t.Helper(); select { case <-e.started: case <-time.After(time.Second): t.Fatal("executor did not start") } }
type alwaysSuccessExecutor struct{}
func newAlwaysSuccessExecutor() alwaysSuccessExecutor { return alwaysSuccessExecutor{} }
func (alwaysSuccessExecutor) Execute(context.Context, ExecutionRequest) ExecutionResponse { return ExecutionResponse{Kind: ResultSuccess} }

func mustCompile(t *testing.T, definition WorkflowDefinition) *CompiledWorkflow { t.Helper(); compiled, err := Compile(definition); if err != nil { t.Fatal(err) }; return compiled }
func newTestEngine(store Store, executor Executor) *Engine { return newTestEngineWithClock(store, executor, RealClock{}) }
func newTestEngineWithClock(store Store, executor Executor, clock Clock) *Engine { engine, err := NewEngine(store, executor, EngineOptions{Clock: clock, NewRunID: func() (RunID, error) { return "run-one", nil }}); if err != nil { panic(err) }; return engine }
func independentWorkflow(tasks, concurrency int) WorkflowDefinition { definition := WorkflowDefinition{ID: "independent", Concurrency: concurrency}; for i := 0; i < tasks; i++ { definition.Tasks = append(definition.Tasks, TaskDefinition{Key: TaskKey(fmt.Sprintf("task-%d", i)), Action: fmt.Sprintf("task-%d", i), TimeoutMillis: 1000}) }; return definition }
func linearWorkflow(tasks int) WorkflowDefinition { definition := WorkflowDefinition{ID: "linear", Concurrency: 1}; for i := 0; i < tasks; i++ { task := TaskDefinition{Key: TaskKey(fmt.Sprintf("task-%d", i)), Action: "ok", TimeoutMillis: 1000}; if i > 0 { task.DependsOn = []TaskKey{TaskKey(fmt.Sprintf("task-%d", i-1))} }; definition.Tasks = append(definition.Tasks, task) }; return definition }
func createSingleTaskRun(t *testing.T, engine *Engine, retry RetryPolicy, timeout int64) RunID { t.Helper(); return createRun(t, engine, WorkflowDefinition{ID: "single", Concurrency: 1, Tasks: []TaskDefinition{{Key: "a", Action: "a", Retry: retry, TimeoutMillis: timeout}}}) }
func createTwoTaskRun(t *testing.T, engine *Engine, retry RetryPolicy) RunID { t.Helper(); return createRun(t, engine, WorkflowDefinition{ID: "two", Concurrency: 1, Tasks: []TaskDefinition{{Key: "a", Action: "a", Retry: retry, TimeoutMillis: 1000}, {Key: "b", Action: "b", DependsOn: []TaskKey{"a"}, TimeoutMillis: 1000}}}) }
func createRun(t *testing.T, engine *Engine, definition WorkflowDefinition) RunID { t.Helper(); id, err := engine.CreateRun(context.Background(), mustCompile(t, definition)); if err != nil { t.Fatal(err) }; return id }
type runResult struct { run WorkflowRun; err error }
func executeAsync(engine *Engine, id RunID) <-chan runResult { done := make(chan runResult, 1); go func() { run, err := engine.Execute(context.Background(), id); done <- runResult{run: run, err: err} }(); return done }
func receiveRun(t *testing.T, done <-chan runResult) WorkflowRun { t.Helper(); select { case result := <-done: if result.err != nil { t.Fatal(result.err) }; return result.run; case <-time.After(time.Second): t.Fatal("engine did not finish"); return WorkflowRun{} } }
```

恢复专用的 `runningSnapshotWithOneAttempt` 与 `snapshotWithSucceededParentAndCorruptRemainingCount` 在任务 8 添加，因为它们依赖恢复语义；CLI 的 `createSucceededRunForCLI` 在 `cmd/workload/main_test.go` 内定义，不能跨 package 复用。

- [ ] **步骤 1：先写串行、DAG 解锁和并发上限失败测试**

```go
func TestEngineExecutesDAGAfterDependenciesSucceed(t *testing.T) {
	store := newMemoryStore()
	executor := newRecordingExecutor(map[string]ExecutionResponse{
		"a": {Kind: ResultSuccess}, "b": {Kind: ResultSuccess}, "c": {Kind: ResultSuccess},
	})
	engine := newTestEngine(store, executor)
	compiled := mustCompile(t, WorkflowDefinition{ID: "w", Concurrency: 2, Tasks: []TaskDefinition{
		{Key: "a", Action: "a", TimeoutMillis: 1000},
		{Key: "b", Action: "b", DependsOn: []TaskKey{"a"}, TimeoutMillis: 1000},
		{Key: "c", Action: "c", DependsOn: []TaskKey{"a"}, TimeoutMillis: 1000},
	}})
	id, err := engine.CreateRun(context.Background(), compiled)
	if err != nil { t.Fatal(err) }
	run, err := engine.Execute(context.Background(), id)
	if err != nil { t.Fatal(err) }
	if run.Status != WorkflowSucceeded { t.Fatalf("status = %s", run.Status) }
	if executor.startedBefore("b", "a") || executor.startedBefore("c", "a") {
		t.Fatal("downstream task started before dependency succeeded")
	}
}

func TestEngineNeverExceedsConcurrencyLimit(t *testing.T) {
	store := newMemoryStore()
	executor := newGateExecutor()
	engine := newTestEngine(store, executor)
	compiled := mustCompile(t, independentWorkflow(5, 2))
	id, _ := engine.CreateRun(context.Background(), compiled)
	done := make(chan error, 1)
	go func() { _, err := engine.Execute(context.Background(), id); done <- err }()
	executor.waitForStarted(t, 2)
	if got := executor.maxActive(); got != 2 { t.Fatalf("max active = %d, want 2", got) }
	executor.releaseAll()
	if err := <-done; err != nil { t.Fatal(err) }
}
```

- [ ] **步骤 2：确认测试因 Engine API 缺失而失败**

运行：`go test ./workflow -run 'TestEngineExecutesDAG|TestEngineNeverExceeds' -v`

预期：编译失败，包含 `undefined: NewEngine` 或 `undefined: EngineOptions`。

- [ ] **步骤 3：实现 Engine 构造、RunID 和初始持久化**

先在 `workflow/engine_test.go` 增加 RunID 格式测试：

```go
func TestRandomRunIDUses32HexCharacters(t *testing.T) {
	id, err := randomRunID()
	if err != nil { t.Fatal(err) }
	if len(id) != 32 { t.Fatalf("RunID length = %d, want 32", len(id)) }
	if _, err := hex.DecodeString(string(id)); err != nil { t.Fatalf("RunID is not hexadecimal: %v", err) }
}
```

在测试 import 中加入 `encoding/hex`，先运行：`go test ./workflow -run TestRandomRunIDUses32HexCharacters -v`

预期：编译失败，包含 `undefined: randomRunID`，证明随机 RunID 还没有实现。

```go
// EngineOptions 提供时钟和 RunID 生成器等可测试依赖。
type EngineOptions struct {
	Clock    Clock
	NewRunID func() (RunID, error)
}

// Engine 串行提交运行状态，并在并发上限内协调 Executor。
type Engine struct {
	store    Store
	executor Executor
	clock    Clock
	newRunID func() (RunID, error)
	mu       sync.Mutex
	active   map[RunID]context.CancelFunc
	compiled map[RunID]*CompiledWorkflow
}

// NewEngine 使用显式 Store、Executor 和可选依赖创建工作流内核。
func NewEngine(store Store, executor Executor, options EngineOptions) (*Engine, error) {
	if store == nil || executor == nil {
		return nil, fmt.Errorf("store and executor are required")
	}
	if options.Clock == nil { options.Clock = RealClock{} }
	if options.NewRunID == nil { options.NewRunID = randomRunID }
	return &Engine{store: store, executor: executor, clock: options.Clock, newRunID: options.NewRunID, active: make(map[RunID]context.CancelFunc), compiled: make(map[RunID]*CompiledWorkflow)}, nil
}

// CreateRun 持久化独立运行快照，并缓存可共享的只读编译结果。
func (e *Engine) CreateRun(ctx context.Context, compiled *CompiledWorkflow) (RunID, error) {
	if compiled == nil { return "", fmt.Errorf("compiled workflow is required") }
	id, err := e.newRunID()
	if err != nil { return "", err }
	snapshot := newRunSnapshot(id, compiled, e.clock.Now())
	if err := e.store.Create(ctx, snapshot); err != nil { return "", err }
	e.mu.Lock()
	e.compiled[id] = compiled
	e.mu.Unlock()
	return id, nil
}

func randomRunID() (RunID, error) {
	var raw [16]byte
	if _, err := rand.Read(raw[:]); err != nil { return "", fmt.Errorf("generate run id: %w", err) }
	return RunID(hex.EncodeToString(raw[:])), nil
}
```

在 `workflow/engine.go` 的 import 中加入 `crypto/rand` 和 `encoding/hex`。随后运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestRandomRunIDUses32HexCharacters|TestEngineExecutesDAG|TestEngineNeverExceeds' -v`

预期：全部 PASS；生产路径使用 `crypto/rand.Read` 生成 16 字节并用 `encoding/hex` 编码，不使用时间戳或全局自增值；调度测试通过 `NewRunID` 注入固定 ID，避免测试依赖随机值。

- [ ] **步骤 4：实现仅处理成功结果的事件循环**

`Execute` 先加载快照；同一 Engine 创建的 Run 优先复用 `compiled` 中的不可变编译结果，进程重启或缓存缺失时才从快照中的 Definition 重新编译。缓存只保存定义、TaskKey 索引和 DAG 邻接表，每个 Run 的状态与依赖计数仍在独立快照中。

```go
func (e *Engine) compiledForRun(id RunID, snapshot RunSnapshot) (*CompiledWorkflow, error) {
	e.mu.Lock()
	compiled := e.compiled[id]
	e.mu.Unlock()
	if compiled != nil { return compiled, nil }
	if snapshot.Definition == nil { return nil, fmt.Errorf("run %q has no stored definition", id) }
	compiled, err := Compile(*snapshot.Definition)
	if err != nil { return nil, fmt.Errorf("compile stored definition: %w", err) }
	e.mu.Lock()
	e.compiled[id] = compiled
	e.mu.Unlock()
	return compiled, nil
}

func (e *Engine) forgetCompiled(id RunID) {
	e.mu.Lock()
	delete(e.compiled, id)
	e.mu.Unlock()
}

func cloneRunSnapshot(snapshot RunSnapshot) RunSnapshot {
	clone := snapshot
	clone.Run.Tasks = append([]TaskRun(nil), snapshot.Run.Tasks...)
	for i := range clone.Run.Tasks { clone.Run.Tasks[i].Attempts = append([]Attempt(nil), snapshot.Run.Tasks[i].Attempts...) }
	clone.Run.RemainingDependencies = append([]int(nil), snapshot.Run.RemainingDependencies...)
	clone.Events = append([]StateEvent(nil), snapshot.Events...)
	return clone
}
```

事件循环必须：将 Workflow 从 pending 转为 running 并保存；按 Task 数组下标启动 ready 任务；启动前在快照副本中创建 Attempt、转为 running，保存成功后才启动 Executor；最多保留 `compiled.definition.Concurrency` 个 goroutine；结果通过单一 channel 回到事件循环；成功状态保存后才递减直接下游计数；计数第一次变零时将下游转为 ready；全部任务成功后保存 WorkflowSucceeded。`Execute` 在返回终态前调用 `forgetCompiled`，系统错误保留缓存以便同进程重试。

结果消息使用固定结构：

```go
type executionCompletion struct {
	taskIndex int
	attempt   int
	response  ExecutionResponse
	finished  time.Time
}
```

任务 5 遇到非成功结果时返回明确的 `unsupported execution result` 系统错误；任务 6 用失败测试替换该临时分支。

- [ ] **步骤 5：运行成功路径、并发和重复成功保护测试**

增加 `TestEngineDoesNotUnlockDependencyTwice`。将完成消息的状态更新抽成不依赖 goroutine 的内部处理函数（例如 `applyCompletion`），在测试中对同一 `taskIndex/attempt` 调用两次，断言 `RemainingDependencies` 不会减到负数且下游只启动一次；测试不通过修改生产 channel 或依赖竞态时序来伪造重复结果。

运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestEngine' -v && go test ./... && go vet ./...`

预期：全部 PASS。

- [ ] **步骤 6：暂停供用户审核并提示手动提交**

建议 commit：`实现工作流成功路径调度与并发控制`

---

### 任务 6：实现临时失败、永久失败、重试、超时和失败传播

**文件：**

- 修改：`workflow/engine.go`
- 修改：`workflow/engine_test.go`

**接口：**

- 消费：TaskDefinition.Retry、TimeoutMillis、Executor 的 ResultKind 和 Clock。
- 产生：Attempt 的 failed/timed_out 结果、TaskWaitingRetry/TaskFailed、下游 TaskSkipped 和最终 WorkflowFailed。

- [ ] **步骤 1：先写临时失败重试和重试耗尽失败测试**

```go
func TestEngineRetriesTemporaryFailureThenSucceeds(t *testing.T) {
	clock := newManualClock(time.Unix(100, 0))
	store := newMemoryStore()
	executor := newSequenceExecutor([]ExecutionResponse{
		{Kind: ResultTemporaryFailure, ErrorCode: "busy"},
		{Kind: ResultSuccess, Output: "ok"},
	})
	engine := newTestEngineWithClock(store, executor, clock)
	id := createSingleTaskRun(t, engine, RetryPolicy{MaxAttempts: 2, IntervalMillis: 1000}, 5000)
	done := executeAsync(engine, id)
	executor.waitForCalls(t, 1)
	clock.waitForTimers(t, 1)
	clock.Advance(time.Second)
	run := receiveRun(t, done)
	if run.Status != WorkflowSucceeded || len(run.Tasks[0].Attempts) != 2 {
		t.Fatalf("run = %+v", run)
	}
}

func TestEngineExhaustsRetryAndSkipsDescendants(t *testing.T) {
	clock := newManualClock(time.Unix(100, 0))
	store := newMemoryStore()
	executor := newSequenceExecutor([]ExecutionResponse{
		{Kind: ResultTemporaryFailure}, {Kind: ResultTemporaryFailure},
	})
	engine := newTestEngineWithClock(store, executor, clock)
	id := createTwoTaskRun(t, engine, RetryPolicy{MaxAttempts: 2, IntervalMillis: 1000})
	done := executeAsync(engine, id)
	executor.waitForCalls(t, 1)
	clock.waitForTimers(t, 1)
	clock.Advance(time.Second)
	run := receiveRun(t, done)
	if run.Status != WorkflowFailed { t.Fatalf("status = %s", run.Status) }
	if run.Tasks[0].Status != TaskFailed || run.Tasks[1].Status != TaskSkipped {
		t.Fatalf("task statuses = %s, %s", run.Tasks[0].Status, run.Tasks[1].Status)
	}
}
```

- [ ] **步骤 2：确认旧 Engine 因 unsupported result 失败**

运行：`go test ./workflow -run 'TestEngineRetriesTemporary|TestEngineExhaustsRetry' -v`

预期：FAIL，错误包含 `unsupported execution result`，证明测试经过 RED。

- [ ] **步骤 3：实现错误分类和固定间隔重试**

处理完成消息时固定使用以下映射，不引入未定义的中间状态：

| Executor 结果 | Attempt 终态 | Task 后续状态 | 是否重试 |
| --- | --- | --- | --- |
| `ResultSuccess` | `AttemptSucceeded` | `TaskSucceeded`，保存成功后解锁直接下游 | 否 |
| `ResultTemporaryFailure` 且仍有次数 | `AttemptFailed` | `TaskWaitingRetry`，设置 `ReadyAt` | 是 |
| `ResultTemporaryFailure` 且次数耗尽 | `AttemptFailed` | `TaskFailed`，后代变为 `TaskSkipped` | 否 |
| `ResultPermanentFailure` | `AttemptFailed` | `TaskFailed`，后代变为 `TaskSkipped` | 否 |
| 其他 ResultKind | 不改变已保存状态 | Execute 返回系统错误 | 否 |

实现时先用 `cloneRunSnapshot` 深拷贝当前快照，在副本上调用 `transitionAttempt` 和 `transitionTask`；将 `ExecutionResponse` 的三个结果字段复制到 `Attempt.Result`。成功分支只遍历 `compiled.successors[taskIndex]`，每个计数只允许从正数减一，变为零时把 waiting_dependencies 转为 ready。失败分支以失败任务的直接下游为队列，沿 `compiled.successors` 广度遍历，只把非终态后代转为 skipped。副本保存成功后才替换事件循环当前快照。

事件循环没有 ready 任务但存在 waiting_retry 时，只等待最早 ReadyAt；到期后将对应任务转为 TaskReady。固定间隔等待必须使用 Clock，不调用 `time.Sleep`。

- [ ] **步骤 4：先写超时失败测试，再实现 AttemptTimedOut**

```go
func TestEngineTimesOutAttemptAndRetries(t *testing.T) {
	clock := newManualClock(time.Unix(100, 0))
	store := newMemoryStore()
	executor := newBlockingThenSuccessExecutor()
	engine := newTestEngineWithClock(store, executor, clock)
	id := createSingleTaskRun(t, engine, RetryPolicy{MaxAttempts: 2}, 1000)
	done := executeAsync(engine, id)
	executor.waitForCalls(t, 1)
	clock.waitForTimers(t, 1)
	clock.Advance(time.Second)
	run := receiveRun(t, done)
	if run.Status != WorkflowSucceeded { t.Fatalf("status = %s", run.Status) }
	if run.Tasks[0].Attempts[0].Status != AttemptTimedOut {
		t.Fatalf("first attempt = %s", run.Tasks[0].Attempts[0].Status)
	}
}
```

运行测试确认第一轮 FAIL；随后每个 Attempt 创建独立 cancel context，并在事件循环中同时等待 Executor 完成和 `clock.After(timeout)`。超时先取消执行器，再把 Attempt 记为 timed_out，按重试策略处理；迟到结果必须根据 Attempt Number 丢弃，不能覆盖新 Attempt。

- [ ] **步骤 5：补充永久失败、分叉 DAG 和全部失败路径测试**

必须覆盖：永久失败只执行一次；失败沿多层后代传播 TaskSkipped；与失败分支无依赖的独立分支仍可结束，但 Workflow 最终为 failed；失败任务的 ErrorCode/ErrorMessage 被保存在 Attempt.Result；状态保存成功后才开始重试或下游传播。

- [ ] **步骤 6：运行失败路径测试和全量回归**

运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestEngineRetries|TestEngineExhausts|TestEngineTimesOut|TestEnginePermanent|TestEngineSkips' -v && go test ./... && go vet ./...`

预期：全部 PASS。

- [ ] **步骤 7：暂停供用户审核并提示手动提交**

建议 commit：`实现任务重试超时与失败传播`

---

### 任务 7：实现协作式取消和系统错误停机

**文件：**

- 修改：`workflow/engine.go`
- 修改：`workflow/engine_test.go`

**接口：**

- `Cancel(context.Context, RunID) error`。
- 系统错误直接从 Execute 返回；未持久化成功时不推进内存状态和新任务。

- [ ] **步骤 1：先写运行中取消失败测试**

```go
func TestEngineCancelStopsRunningAndCancelsPendingTasks(t *testing.T) {
	store := newMemoryStore()
	executor := newCancelAwareExecutor()
	engine := newTestEngine(store, executor)
	id := createTwoTaskRun(t, engine, RetryPolicy{MaxAttempts: 3})
	done := executeAsync(engine, id)
	executor.waitUntilStarted(t)
	if err := engine.Cancel(context.Background(), id); err != nil { t.Fatal(err) }
	run := receiveRun(t, done)
	if run.Status != WorkflowCanceled { t.Fatalf("status = %s", run.Status) }
	if run.Tasks[0].Status != TaskCanceled || run.Tasks[1].Status != TaskCanceled {
		t.Fatalf("task statuses = %s, %s", run.Tasks[0].Status, run.Tasks[1].Status)
	}
	if len(run.Tasks[0].Attempts) != 1 || run.Tasks[0].Attempts[0].Status != AttemptCanceled {
		t.Fatalf("attempts = %+v", run.Tasks[0].Attempts)
	}
}
```

- [ ] **步骤 2：确认测试因 Cancel 缺失而失败**

运行：`go test ./workflow -run TestEngineCancelStopsRunning -v`

预期：编译失败，包含 `engine.Cancel undefined`。

- [ ] **步骤 3：实现活动 Run 注册和取消请求**

Execute 创建 `runContext, cancel := context.WithCancel(ctx)` 后，在 Engine.active 中按 RunID 注册；defer 删除并释放。Cancel 在互斥锁下取得 cancel 函数，存在时调用并返回；Run 未活动时加载快照，终态返回 nil，非终态将所有未开始任务转为 canceled、Workflow 转为 canceled 并保存。执行器因 runContext 取消返回时，当前 Attempt 记为 canceled，不触发重试。

- [ ] **步骤 4：先写持久化失败测试**

```go
func TestEngineDoesNotStartExecutorWhenRunningStateCannotBeSaved(t *testing.T) {
	store := newFailingStore(2) // Create 成功，第一次 Save 失败
	executor := newRecordingExecutor(nil)
	engine := newTestEngine(store, executor)
	id := createSingleTaskRun(t, engine, RetryPolicy{MaxAttempts: 1}, 1000)
	if _, err := engine.Execute(context.Background(), id); err == nil {
		t.Fatal("Execute() error = nil, want store failure")
	}
	if executor.callCount() != 0 {
		t.Fatalf("executor calls = %d, want 0", executor.callCount())
	}
}

func TestEngineDoesNotUnlockSuccessorWhenSuccessCannotBeSaved(t *testing.T) {
	store := newFailingStore(4) // Create、WorkflowRunning、AttemptRunning 成功，结果 Save 失败
	executor := newRecordingExecutor(map[string]ExecutionResponse{"a": {Kind: ResultSuccess}})
	engine := newTestEngine(store, executor)
	id := createTwoTaskRun(t, engine, RetryPolicy{MaxAttempts: 1})
	if _, err := engine.Execute(context.Background(), id); err == nil {
		t.Fatal("Execute() error = nil, want store failure")
	}
	if executor.wasCalled("b") { t.Fatal("successor ran after persistence failure") }
}
```

- [ ] **步骤 5：实现写前复制和保存后提交**

每次状态变化先深拷贝当前 RunSnapshot，在副本上完成状态、Attempt、事件和依赖计数修改；`Store.Save` 成功后才用副本替换事件循环中的当前状态。任何 Store 错误立即取消所有活动 Attempt、停止启动新任务并从 Execute 返回包装后的系统错误。

- [ ] **步骤 6：运行取消、存储失败和全量测试**

运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestEngineCancel|TestEngineDoesNot' -v && go test ./... && go vet ./...`

预期：全部 PASS。

- [ ] **步骤 7：暂停供用户审核并提示手动提交**

建议 commit：`实现工作流取消与持久化失败保护`

---

### 任务 8：实现进程重启恢复和至少执行一次语义

**文件：**

- 新建：`workflow/recovery.go`
- 新建：`workflow/recovery_test.go`
- 修改：`workflow/engine.go`

**接口：**

- `Resume(context.Context, RunID) (WorkflowRun, error)`。
- 恢复时重新 Compile Definition、重算依赖计数、把 running Attempt 记为 interrupted，并按剩余次数重试或失败。

- [ ] **步骤 1：先写执行中 Attempt 恢复失败测试**

```go
func TestResumeMarksRunningAttemptInterruptedAndRetries(t *testing.T) {
	store := newMemoryStore()
	snapshot := runningSnapshotWithOneAttempt(t, "run-one", RetryPolicy{MaxAttempts: 2})
	store.snapshots["run-one"] = snapshot
	executor := newSequenceExecutor([]ExecutionResponse{{Kind: ResultSuccess}})
	engine := newTestEngine(store, executor)
	run, err := engine.Resume(context.Background(), "run-one")
	if err != nil { t.Fatal(err) }
	if run.Status != WorkflowSucceeded { t.Fatalf("status = %s", run.Status) }
	if got := run.Tasks[0].Attempts[0].Status; got != AttemptInterrupted {
		t.Fatalf("first attempt = %s, want interrupted", got)
	}
	if len(run.Tasks[0].Attempts) != 2 { t.Fatalf("attempt count = %d", len(run.Tasks[0].Attempts)) }
}

func TestResumeRecomputesDependenciesInsteadOfTrustingSavedCount(t *testing.T) {
	store := newMemoryStore()
	snapshot := snapshotWithSucceededParentAndCorruptRemainingCount(t, "run-one", 99)
	store.snapshots["run-one"] = snapshot
	engine := newTestEngine(store, newAlwaysSuccessExecutor())
	run, err := engine.Resume(context.Background(), "run-one")
	if err != nil { t.Fatal(err) }
	if run.Tasks[1].Status != TaskSucceeded { t.Fatalf("child status = %s", run.Tasks[1].Status) }
}
```

在同一个 `workflow/recovery_test.go` 中定义测试快照工厂，避免依赖隐藏夹具：

```go
func runningSnapshotWithOneAttempt(t *testing.T, id RunID, retry RetryPolicy) RunSnapshot {
	t.Helper()
	compiled := mustCompile(t, WorkflowDefinition{ID: "resume-one", Concurrency: 1, Tasks: []TaskDefinition{{Key: "a", Action: "a", Retry: retry, TimeoutMillis: 1000}}})
	snapshot := newRunSnapshot(id, compiled, time.Unix(1, 0))
	started := time.Unix(2, 0)
	snapshot.Run.Status = WorkflowRunning
	snapshot.Run.StartedAt = &started
	snapshot.Run.Tasks[0].Status = TaskRunning
	snapshot.Run.Tasks[0].Attempts = []Attempt{{Number: 1, Status: AttemptRunning, StartedAt: started}}
	return snapshot
}

func snapshotWithSucceededParentAndCorruptRemainingCount(t *testing.T, id RunID, corrupt int) RunSnapshot {
	t.Helper()
	compiled := mustCompile(t, WorkflowDefinition{ID: "resume-dag", Concurrency: 1, Tasks: []TaskDefinition{
		{Key: "a", Action: "a", TimeoutMillis: 1000},
		{Key: "b", Action: "b", DependsOn: []TaskKey{"a"}, TimeoutMillis: 1000},
	}})
	snapshot := newRunSnapshot(id, compiled, time.Unix(1, 0))
	started, finished := time.Unix(2, 0), time.Unix(3, 0)
	snapshot.Run.Status, snapshot.Run.StartedAt = WorkflowRunning, &started
	snapshot.Run.Tasks[0].Status, snapshot.Run.Tasks[0].FinishedAt = TaskSucceeded, &finished
	snapshot.Run.Tasks[0].Attempts = []Attempt{{Number: 1, Status: AttemptSucceeded, StartedAt: started, FinishedAt: &finished}}
	snapshot.Run.Tasks[1].Status = TaskWaitingDependencies
	snapshot.Run.RemainingDependencies[1] = corrupt
	return snapshot
}
```

- [ ] **步骤 2：确认测试因 Resume 缺失而失败**

运行：`go test ./workflow -run 'TestResume' -v`

预期：编译失败，包含 `engine.Resume undefined`。

- [ ] **步骤 3：实现恢复预处理**

```go
// Resume 从持久化快照重建运行时计数，并按至少执行一次语义继续运行。
func (e *Engine) Resume(ctx context.Context, id RunID) (WorkflowRun, error) {
	snapshot, err := e.store.Load(ctx, id)
	if err != nil { return WorkflowRun{}, err }
	if snapshot.Definition == nil { return WorkflowRun{}, fmt.Errorf("run %q has no stored definition", id) }
	compiled, err := Compile(*snapshot.Definition)
	if err != nil { return WorkflowRun{}, fmt.Errorf("compile stored definition: %w", err) }
	if isWorkflowTerminal(snapshot.Run.Status) { return snapshot.Run, nil }
	now := e.clock.Now()
	if err := prepareForResume(&snapshot, compiled, now); err != nil {
		return WorkflowRun{}, err
	}
	if err := e.store.Save(ctx, snapshot); err != nil { return WorkflowRun{}, err }
	e.mu.Lock()
	e.compiled[id] = compiled
	e.mu.Unlock()
	run, err := e.executeSnapshot(ctx, compiled, snapshot)
	if err == nil && isWorkflowTerminal(run.Status) { e.forgetCompiled(id) }
	return run, err
}
```

`prepareForResume` 按下列固定顺序实现，且每一轮状态变化都通过任务 2 的转换函数记录事件：

```go
func prepareForResume(snapshot *RunSnapshot, compiled *CompiledWorkflow, now time.Time) error {
	if len(snapshot.Run.Tasks) != len(compiled.definition.Tasks) { return fmt.Errorf("stored task count does not match definition") }
	// 保存的计数可能停留在崩溃前的中间状态，恢复时必须由 DAG 和任务终态重新推导。
	snapshot.Run.RemainingDependencies = make([]int, len(snapshot.Run.Tasks))
	for i := range snapshot.Run.Tasks {
		task := &snapshot.Run.Tasks[i]
		if task.Status == TaskRunning {
			if len(task.Attempts) == 0 || task.Attempts[len(task.Attempts)-1].Status != AttemptRunning { return fmt.Errorf("task %q is running without running attempt", task.Key) }
			attempt := &task.Attempts[len(task.Attempts)-1]
			if err := transitionAttempt(snapshot, task.Key, attempt, AttemptInterrupted, now, "process restarted"); err != nil { return err }
			if len(task.Attempts) >= compiled.definition.Tasks[i].Retry.MaxAttempts {
				if err := transitionTask(snapshot, i, TaskFailed, now, "attempts exhausted after restart"); err != nil { return err }
			} else {
				if err := transitionTask(snapshot, i, TaskWaitingRetry, now, "retry interrupted attempt"); err != nil { return err }
				task.ReadyAt = &now
			}
		}
	}
	// 先传播恢复阶段已经确定的终态失败，避免任务数组顺序不是拓扑顺序时遗漏后代。
	for i := range snapshot.Run.Tasks {
		if status := snapshot.Run.Tasks[i].Status; status == TaskFailed || status == TaskCanceled || status == TaskSkipped {
			skipDescendants(snapshot, compiled, i, now, "upstream terminal failure")
		}
	}
	for i := range snapshot.Run.Tasks {
		if isTaskTerminal(snapshot.Run.Tasks[i].Status) { continue }
		remaining := 0
		blocked := false
		for _, dependency := range compiled.dependencies[i] {
			switch snapshot.Run.Tasks[dependency].Status {
			case TaskSucceeded:
			case TaskFailed, TaskCanceled, TaskSkipped: blocked = true
			default: remaining++
			}
		}
		snapshot.Run.RemainingDependencies[i] = remaining
		if blocked { if err := transitionTask(snapshot, i, TaskSkipped, now, "upstream terminal failure"); err != nil { return err }; continue }
		if remaining == 0 && snapshot.Run.Tasks[i].Status == TaskWaitingDependencies {
			if err := transitionTask(snapshot, i, TaskReady, now, "dependencies restored"); err != nil { return err }
		}
	}
	return nil
}
```

为使上述代码明确可编译，`isTaskTerminal` 返回 succeeded、failed、canceled 或 skipped；任务 2 的 `transitionTask` 最终签名固定为 `transitionTask(snapshot *RunSnapshot, taskIndex int, to TaskStatus, at time.Time, reason string) error`。

- [ ] **步骤 4：补充至少执行一次、终态幂等和恢复失败测试**

必须覆盖：执行器可能已成功但快照仍为 running 时恢复会再次执行；已成功任务不重跑；已取消和已失败 Run 不自动恢复；损坏定义恢复失败且不调用 Executor；被中断 Attempt 计入最大尝试次数。

- [ ] **步骤 5：运行恢复测试和全量回归**

运行：`gofmt -w workflow/*.go && go test ./workflow -run 'TestResume' -v && go test ./... && go vet ./...`

预期：全部 PASS。

- [ ] **步骤 6：暂停供用户审核并提示手动提交**

建议 commit：`实现工作流重启恢复语义`

---

### 任务 9：实现最小 CLI 和公开示例

**文件：**

- 新建：`cmd/workload/main.go`
- 新建：`cmd/workload/main_test.go`
- 新建：`examples/document-pipeline.json`
- 新建：`.gitignore`

**接口：**

- `workload run <workflow.json>`：读取、编译、创建并前台执行，输出 RunID 和终态。
- `workload status <run-id>`：只读取快照并输出 JSON，不直接修改文件。
- `WORKLOAD_DATA_DIR`：覆盖默认 `.workload/runs`。

- [ ] **步骤 1：先写 CLI 参数和运行失败测试**

```go
package main

import (
	"bytes"
	"context"
	"path/filepath"
	"testing"
)

func TestRunCommandExecutesExampleAndPrintsRunID(t *testing.T) {
	dataDir := t.TempDir()
	var stdout, stderr bytes.Buffer
	exitCode := run(context.Background(), []string{"run", filepath.Join("..", "..", "examples", "document-pipeline.json")}, dataDir, &stdout, &stderr)
	if exitCode != 0 { t.Fatalf("exit = %d, stderr = %s", exitCode, stderr.String()) }
	if !bytes.Contains(stdout.Bytes(), []byte("run_id=")) || !bytes.Contains(stdout.Bytes(), []byte("status=succeeded")) {
		t.Fatalf("stdout = %q", stdout.String())
	}
}

func TestStatusCommandReadsPersistedSnapshot(t *testing.T) {
	dataDir := t.TempDir()
	runID := createSucceededRunForCLI(t, dataDir)
	var stdout, stderr bytes.Buffer
	exitCode := run(context.Background(), []string{"status", string(runID)}, dataDir, &stdout, &stderr)
	if exitCode != 0 { t.Fatalf("exit = %d, stderr = %s", exitCode, stderr.String()) }
	if !bytes.Contains(stdout.Bytes(), []byte(`"status":"succeeded"`)) { t.Fatalf("stdout = %q", stdout.String()) }
}
```

- [ ] **步骤 2：确认测试因 run 函数和示例缺失而失败**

运行：`go test ./cmd/workload -v`

预期：编译失败，包含 `undefined: run`，或示例文件不存在。

- [ ] **步骤 3：实现可测试的 CLI 入口**

```go
func main() {
	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
	defer stop()
	dataDir := os.Getenv("WORKLOAD_DATA_DIR")
	if dataDir == "" { dataDir = filepath.Join(".workload", "runs") }
	os.Exit(run(ctx, os.Args[1:], dataDir, os.Stdout, os.Stderr))
}

func run(ctx context.Context, args []string, dataDir string, stdout, stderr io.Writer) int {
	if len(args) != 2 || (args[0] != "run" && args[0] != "status") {
		fmt.Fprintln(stderr, "usage: workload run <workflow.json> | workload status <run-id>")
		return 2
	}
	store, err := filestore.New(dataDir)
	if err != nil { fmt.Fprintln(stderr, err); return 1 }
	if args[0] == "status" {
		snapshot, err := store.Load(ctx, workflow.RunID(args[1]))
		if err != nil { fmt.Fprintln(stderr, err); return 1 }
		if err := json.NewEncoder(stdout).Encode(snapshot.Run); err != nil { fmt.Fprintln(stderr, err); return 1 }
		return 0
	}
	definition, err := readDefinition(args[1])
	if err != nil { fmt.Fprintln(stderr, err); return 1 }
	compiled, err := workflow.Compile(definition)
	if err != nil { fmt.Fprintln(stderr, err); return 1 }
	executor := mockexec.New(workflow.RealClock{}, successScripts(definition))
	engine, err := workflow.NewEngine(store, executor, workflow.EngineOptions{})
	if err != nil { fmt.Fprintln(stderr, err); return 1 }
	id, err := engine.CreateRun(ctx, compiled)
	if err != nil { fmt.Fprintln(stderr, err); return 1 }
	runState, err := engine.Execute(ctx, id)
	if err != nil { fmt.Fprintln(stderr, err); return 1 }
	fmt.Fprintf(stdout, "run_id=%s status=%s\n", id, runState.Status)
	return 0
}

func readDefinition(path string) (workflow.WorkflowDefinition, error) {
	file, err := os.Open(path)
	if err != nil { return workflow.WorkflowDefinition{}, err }
	defer file.Close()
	decoder := json.NewDecoder(file)
	decoder.DisallowUnknownFields()
	var definition workflow.WorkflowDefinition
	if err := decoder.Decode(&definition); err != nil { return workflow.WorkflowDefinition{}, fmt.Errorf("decode workflow: %w", err) }
	var extra any
	if err := decoder.Decode(&extra); !errors.Is(err, io.EOF) { return workflow.WorkflowDefinition{}, fmt.Errorf("workflow file must contain exactly one JSON value") }
	return definition, nil
}

func successScripts(definition workflow.WorkflowDefinition) map[string][]mockexec.Step {
	scripts := make(map[string][]mockexec.Step)
	for _, task := range definition.Tasks {
		scripts[task.Action] = append(scripts[task.Action], mockexec.Step{Kind: workflow.ResultSuccess, Output: "completed:" + task.Action})
	}
	return scripts
}
```

在 `cmd/workload/main_test.go` 中补齐状态测试使用的工厂：

```go
func createSucceededRunForCLI(t *testing.T, dataDir string) workflow.RunID {
	t.Helper()
	var stdout, stderr bytes.Buffer
	example := filepath.Join("..", "..", "examples", "document-pipeline.json")
	if exit := run(context.Background(), []string{"run", example}, dataDir, &stdout, &stderr); exit != 0 { t.Fatalf("run exit = %d, stderr = %s", exit, stderr.String()) }
	fields := strings.Fields(stdout.String())
	for _, field := range fields { if strings.HasPrefix(field, "run_id=") { return workflow.RunID(strings.TrimPrefix(field, "run_id=")) } }
	t.Fatalf("run id missing from %q", stdout.String())
	return ""
}
```

- [ ] **步骤 4：增加 Ctrl+C 等价取消、错误 JSON 和 status 不写文件测试**

测试通过取消传入 run 的 context 模拟 Ctrl+C，不发送真实系统信号；断言运行中 Attempt 为 canceled、未开始任务 canceled、命令返回非零并打印 canceled 说明。错误 JSON、未知字段、缺失 RunID 和非法命令必须返回非零。status 前后比较快照文件内容完全一致。

- [ ] **步骤 5：添加示例和忽略运行数据**

```json
{
  "id": "document-pipeline",
  "concurrency": 2,
  "tasks": [
    {"key": "read", "action": "mock-read", "retry": {"max_attempts": 2, "interval_ms": 100}, "timeout_ms": 3000},
    {"key": "clean", "action": "mock-clean", "depends_on": ["read"], "timeout_ms": 3000},
    {"key": "summarize", "action": "mock-summarize", "depends_on": ["clean"], "timeout_ms": 5000}
  ]
}
```

`.gitignore` 增加一行：`.workload/`。

- [ ] **步骤 6：运行 CLI 自动测试和人工演示**

运行：

```bash
go test ./cmd/workload -v
go run ./cmd/workload run examples/document-pipeline.json
WORKLOAD_DATA_DIR=.workload/runs go run ./cmd/workload status <上一步输出的run-id>
```

预期：测试 PASS；run 输出 `status=succeeded`；status 输出包含三个 succeeded TaskRun 的 JSON。人工命令产生的 `.workload/` 被 Git 忽略。

- [ ] **步骤 7：暂停供用户审核并提示手动提交**

建议 commit：`实现单机工作流CLI与示例`

---

### 任务 10：建立性能基线、完善公开文档并完成模块验收

**文件：**

- 新建：`workflow/benchmark_test.go`
- 新建：`docs/实验/模块1性能基线.md`
- 修改：`README.md`
- 修改：`项目状态.md`

**接口：**

- 输入：10,000 任务、1,000 活跃 Run 和不同快照规模；10,000 任务是 Compile 硬上限，1,000 活跃 Run 只是 Benchmark 输入规模，不增加容量拒绝逻辑。
- 输出：可重复运行的 Benchmark、环境和原始结果；不在用户确认前写入性能承诺。

- [ ] **步骤 1：先写并运行基准函数的发现测试**

```go
func BenchmarkCompileTenThousandTasks(b *testing.B) {
	definition := linearWorkflow(10_000)
	b.ReportAllocs()
	for b.Loop() {
		if _, err := Compile(definition); err != nil { b.Fatal(err) }
	}
}

func BenchmarkCreateOneThousandRunsSharedDefinition(b *testing.B) {
	compiled, err := Compile(linearWorkflow(100))
	if err != nil { b.Fatal(err) }
	b.ReportAllocs()
	for b.Loop() {
		runs := make([]RunSnapshot, 1_000)
		for i := range runs {
			runs[i] = newRunSnapshot(RunID(strconv.Itoa(i)), compiled, time.Unix(1, 0))
			if runs[i].Definition != compiled.definition { b.Fatal("run did not retain shared compiled definition") }
		}
		runtime.KeepAlive(runs)
	}
}

func BenchmarkCreateOneThousandRunsSeparateDefinitions(b *testing.B) {
	definition := linearWorkflow(100)
	b.ReportAllocs()
	for b.Loop() {
		runs := make([]RunSnapshot, 1_000)
		seen := make(map[*WorkflowDefinition]struct{}, 1_000)
		for i := range runs {
			compiled, err := Compile(definition)
			if err != nil { b.Fatal(err) }
			runs[i] = newRunSnapshot(RunID(strconv.Itoa(i)), compiled, time.Unix(1, 0))
			seen[runs[i].Definition] = struct{}{}
		}
		if len(seen) != 1_000 { b.Fatalf("definition pointers = %d, want 1000", len(seen)) }
		runtime.KeepAlive(runs)
	}
}
```

这两个基准测量的是“1,000 个运行共享一个不可变编译定义”与“每个运行重复编译并持有独立定义”的真实对象差异，不用内容相等冒充共享。另增加 `BenchmarkEngineCompiledCacheHit`，先由 Engine 创建 Run，再重复调用 `compiledForRun` 并断言返回同一指针；同时增加 `BenchmarkDependencyUnlock`、`BenchmarkFileStoreSnapshot100Tasks`、`BenchmarkFileStoreSnapshot10000Tasks` 和 `BenchmarkResumeTenThousandTasks`。

- [ ] **步骤 2：确认 Go 能发现全部基准且每个至少执行一次**

运行：`go test ./workflow -run '^$' -bench 'Benchmark(Compile|Create|Dependency|Resume)' -benchtime=1x -benchmem`

预期：列出所有指定 Benchmark，无测试失败。

- [ ] **步骤 3：运行稳定基准并记录原始结果**

运行：

```bash
go version
go env GOOS GOARCH
sysctl -n machdep.cpu.brand_string
sysctl -n hw.memsize
go test ./workflow -run '^$' -bench . -benchmem -count=5
```

在 macOS 记录上述环境；Linux 使用 `lscpu` 和 `/proc/meminfo` 替代 `sysctl`。将未删改的命令输出、输入规模、均值观察和主要瓶颈写入 `docs/实验/模块1性能基线.md`。只记录数据和观察，不写“达到生产级”等结论。

- [ ] **步骤 4：更新 README 的实际运行入口和限制**

README 必须包含：`go test ./...`、`go run ./cmd/workload run examples/document-pipeline.json`、status 查询示例、数据目录说明、当前仅 Mock Executor/JSON Store/macOS 与 Linux 原子替换边界、至少执行一次语义，以及性能报告链接。

- [ ] **步骤 5：运行模块 1 完整验证**

```bash
gofmt -w workflow/*.go workflow/filestore/*.go workflow/mockexec/*.go cmd/workload/*.go
go test ./...
go test -race ./...
go vet ./...
bash scripts/test-verify-docs.sh
bash scripts/verify-docs.sh
git diff --check
```

预期：所有 Go 测试、Race Detector、静态检查和文档检查通过。若 Race Detector 发现数据竞争，先用系统化调试定位并修复，不降低测试覆盖或跳过该用例。

- [ ] **步骤 6：按需求文档逐条复核覆盖关系**

确认以下每项都有对应测试或基准：Workflow ID 规则、空工作流校验、10,000 任务硬上限、TaskKey 规则、Action 非空白校验、`O(V + E)` 编译、随机 RunID 格式、状态转换、依赖解锁、重复成功保护、并发上限、临时/永久失败、固定间隔、超时、取消、失败传播、存储失败停机、JSON 原子替换、执行中 Attempt 恢复、依赖计数重算、至少执行一次、CLI、10,000 任务 Benchmark 和 1,000 活跃 Run Benchmark；不增加 1,001 活跃 Run 容量拒绝测试。

逐文件人工复核导出标识符的 Go Doc 注释，以及 DAG、状态机、并发、持久化、恢复、性能和安全边界的关键注释；确认注释使用中文、以解释原因和不变量为主，并且与最终实现一致。

- [ ] **步骤 7：更新项目状态并暂停供用户最终审核**

只有上述验证确实通过后，才在 `项目状态.md` 记录模块 1 实际实现、测试和基准事实。不得把计划中的结果提前写为完成。向用户提供完整 diff 摘要、验证命令、已知限制和待确认性能阈值；用户人工审核前不提交。

建议 commit：`完成单机可靠工作流内核与性能基线`

---

## 计划自检

### 需求覆盖

- Workflow ID、工作流非空、10,000 个任务硬上限、TaskKey、Action 非空白校验、DAG 编译和 `O(V + E)`：任务 1。
- Workflow、Task、Attempt 状态机和每 Run 独立依赖计数：任务 2。
- JSON 快照、Attempt 历史、状态事件和原子替换：任务 3。
- Mock Executor、取消信号和可控时间：任务 4。
- 依赖调度、串行状态更新和并发上限：任务 5。
- 重试、超时、错误分类和失败传播：任务 6。
- 协作式取消、持久化失败和停止推进：任务 7。
- 重启恢复、依赖重算、被中断 Attempt 和至少执行一次：任务 8。
- 最小 CLI、JSON 示例和 Ctrl+C：任务 9。
- 10,000 任务上限、1,000 Run、快照成本、README 和最终验收：任务 10。
- 中文 Go Doc、关键实现注释和注释准确性人工审核：全局约束和任务 10。

### 明确不实现

- HTTP、数据库、Redis、消息队列、多 Worker、容器、Kubernetes、真实模型、真实命令执行、指数退避、随机抖动、完整日志指标、人工审批和恰好执行一次。
- CLI 不提供跨进程 `cancel` 或 `resume` 命令；Cancel 和 Resume 只作为 Go 内核公开接口及自动化测试能力。
- 不为可能的未来需求预留插件注册中心、分布式锁、事件溯源框架或多数据库适配层。

### 执行入口

本计划获用户批准后，使用 `superpowers:executing-plans` 从任务 1 开始执行。每完成一个任务即运行该任务验证、汇报 diff 和暂停审核；用户明确批准后才进入下一任务。Codex 不自动暂存或提交 Git。
